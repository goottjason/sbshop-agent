package com.sbshop.agent.core.domain.product.dto;

/** ANY includes a product when either content field has never been applied or is old. */
public enum ProductContentAgeField {
	ANY, IMAGES, DETAIL_HTML
}
