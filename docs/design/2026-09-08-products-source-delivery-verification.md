# 소싱 수집·검토 운영 검증 (2026-09-08)

최종 배포 `c01e6527d918b3b21b0c4ce7c582a93d86c85cf7`에서 **수집 10건 모두 READY, 가격·재고 검토 5건 모두 READY, FTN 상세 HTML 단독 검토 READY**를 확인했다. 최초 실패 3건은 재현한 원인을 수정한 후 해소됐다. 전체 콘텐츠 선택 적용은 연결 마켓의 편집 조건에 따라 제한되며, 이를 전체 적용 성공으로 표기하지 않는다.

배포 `abeb0d7a`에서 IHB·VTB·FTN·COK·OCD 상품 각 1개를 실제 API로 수집하고 검토했다. 상품 DB 적용과 외부마켓 쓰기는 호출하지 않았다. 각 단계 전후 전체 상품 상세 필드 해시 및 revision을 비교했고, 5개 상품 모두 변경이 없었다. 수집 성공시각과 적용시각을 구분하며 모든 적용시각은 `null`이었다.

## 최초 운영 결과

| 소싱처 / 상품 ID | 가격·재고 수집 / 검토 | 이미지·HTML 수집 / 검토 |
|---|---|---|
| IHB / 328 | HTTP 403 실패, 검토 제외 | 이미지 3개·HTML 수집 완료, 연결 마켓의 편집 정책으로 전체 선택 검토 제외 |
| VTB / 3170 | 수집 완료, 환율 소수 자릿수 때문에 검토 제외 | 이미지 4개·HTML 수집 완료, 연결 마켓의 편집 정책으로 검토 제외 |
| FTN / 601 | 수집 완료, 환율 소수 자릿수 때문에 검토 제외 | 이미지 4개·HTML 수집 완료, 이미지 포함 검토 제외; 상세 HTML은 개별 편집 가능으로 조회됨 |
| COK / 821 | 명시 품절 상태와 가격 수집 완료, 환율 소수 자릿수 때문에 검토 제외 | 이미지 4개·HTML 수집 완료, 연결 마켓의 편집 정책으로 검토 제외 |
| OCD / 1022 | 수집 서비스 HTTP 422 실패, 검토 제외 | 수집 서비스 HTTP 422 실패, 검토 제외 |

수집 7건이 완료됐지만 전체 필드 선택 검토 10건은 모두 제외됐다. 이 결과를 전체 기능 성공으로 간주하지 않는다. 실패한 3건에는 성공시각이 없고, 실패 응답을 품절·0개로 바꾸지 않았다. 원본 요청 UUID와 결과는 최초 서버 백업에 유지한다. [최초 운영 근거](evidence/2026-09-08-products-source-delivery-initial.json)

## 원인 확인과 수정

운영 API 컨테이너에서 공개 소싱 응답만 조회하는 독립 Java 진단을 실행했다. OCD의 동일 요청은 Java 기본 HTTP/2 업그레이드에서 422(`body` 누락), HTTP/1.1에서는 200이었다. 수집 서비스의 Python HTTP/1.1 직접 요청도 가격·재고/콘텐츠 모두 200이었다. 기존 다른 수집 클라이언트와 같이 OCD 클라이언트를 HTTP/1.1로 고정한다. 소싱 페이지의 202·403을 성공으로 처리하는 변경은 없다.

IHB의 짧은 `Mozilla/5.0` 헤더는 403, 이미 사용 중인 콘텐츠 경로와 동일한 전체 User-Agent는 200이었다. 동일 상품 ID 18566·원문 URL·49,886원·명시 재고 boolean을 확인했다. 가격 수집에 동일한 검증된 헤더를 사용하며 상품 식별·통화·재고 검증은 유지한다.

실제 환율 `1822.551899`는 상품 환율 컬럼 `decimal(10,2)` 및 편집 검증 범위를 넘었다. 저장 시점에만 줄이지 않고, 소싱 원가격 환산 전에 `1822.55`로 HALF_UP 반올림한다. 상품 원가 환산과 `LandedCostCalculator`의 배송비 환산 모두 같은 두 자리 환율을 사용한다. 영속 스냅샷의 `pricingEvidence`에는 원통화 가격·원본 환율·정규화 환율·환산 상품가를 보존하며, 화면의 기존 수집 참고 사항에 원본→정규화 환율을 표시한다. 일반 편집의 환율 제한과 판매가 100원 반올림은 유지한다.

## 회귀 검증

소싱 통합 21건과 인프라 31건, 총 52건이 통과했다. HTTP/1.1 요청에 h2c 헤더가 없음을 확인하고, IHB 실제 응답의 최소 fixture, 네 GBP 소싱처의 환율 선반올림, 저장 가능한 환율 범위, 원본 환율 영속 보존, 검토와 저장의 동일 원가, 기존 스냅샷 JSON 호환을 검사했다. 로컬 검사의 DB 저장은 격리 테스트 DB에서만 수행했다.

## 공동 재배포 후 운영 결과

2026-09-08 02:04:36 KST 검증을 마쳤다. `followup-source-fixes` 폴더에서 별도 UUID로 같은 5개 상품을 다시 수집·검토했고, 최초 10건과 새 수집 ID가 겹치지 않음을 확인했다. 최초 요청과 결과는 보존했다. [최종 운영 근거](evidence/2026-09-08-products-source-delivery-followup.json)

| 소싱처 / 상품 ID | 가격·재고 수집 / 검토 | 검토 원가 / 환율 | 콘텐츠 수집 | 콘텐츠 검토 |
|---|---|---|---|---|
| IHB / 328 | READY / READY | 49,886원 / 1 | 이미지 3개·HTML 2,985자 READY | 전체 선택은 마켓 조건 확인 필요로 제외 |
| VTB / 3170 | READY / READY | 16,768원 / 1822.55 | 이미지 4개·HTML 1,656자 READY | 전체 선택은 마켓 조건 확인 필요로 제외 |
| FTN / 601 | READY / READY | 24,058원 / 1822.55 | 이미지 4개·HTML 1,610자 READY | **상세 HTML만 선택하면 READY**; 이미지 포함은 제외 |
| COK / 821 | READY / READY | 57,210원 / 1822.55 | 이미지 4개·HTML 2,073자 READY | 전체 선택은 마켓 조건 확인 필요로 제외 |
| OCD / 1022 | READY / READY | 9,113원 / 1822.55 | 이미지 1개·HTML 3,598자 READY | 전체 선택은 마켓 조건 확인 필요로 제외 |

GBP 소싱처 네 곳 모두 수집 참고 사항에 원본 `1822.551899 → 1822.55`를 표시했고, 제안된 환율과 검토 명령의 환율이 일치했다. COK는 소싱처가 명시한 품절 상태를 관측했다. 다섯 소싱처 모두 실제 수량은 관측되지 않아 `null`로 유지했다.

FTN의 추가 검토는 `selectedFields=[DETAIL_HTML]`, 변경 필드 `detailHtml` 하나, 제외 이유 없음으로 확인했다. 콘텐츠 전체 선택은 다섯 상품 모두 연결 마켓 정책의 제한을 반환하므로 검증 helper의 종합 상태는 `completed_with_limits`다. 이것은 수집 실패를 숨긴 결과가 아니며, 실제 수집 실패는 0건이다.

모든 수집은 성공시각을 남겼고 적용시각은 `null`이었다. 같은 수집 요청 재전송 10건은 동일 collection ID를 반환했다. 다섯 상품의 모든 상세 필드와 revision 0이 최초 검증 전 값과 동일했다. 상품 적용 API와 외부마켓 쓰기 호출은 0건이며, 영속 수집 스냅샷·검토 기록 및 검토용 이미지 호스팅만 생성했다.
