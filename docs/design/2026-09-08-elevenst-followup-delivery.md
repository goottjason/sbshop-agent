# 11번가 후속 납품 — 2026-09-08

운영 실행 버전은 `16ec51f6a9266f4f9171d1f0c4fc9afb9176c448`이다. API·프론트·수집기·마켓플러스 작업자를 배포했고 상품 2,854개 조회, 실제 JAR 엔티티 48개 read-only schema validate, 네 컨테이너 재시작 0을 확인했다. 기존 마켓플러스 브라우저는 교체하지 않았다.

- 판매중·단일 재고·현재/목표 양수에 한정한 11번가 검토형 수량 반영을 추가했다. 정확한 재고번호, 현재 무게 보존, 별도 재조회, 429·업무 오류·응답 유실 방어를 포함한다. 0개·품절 해제·판매 재개는 미개방이다.
- 11번가 등록 입력 준비는 현재 계정 주소 선택·필수값 점검·JSON 보관/불러오기를 제공한다. 계정·revision·입력 변경 시 이전 점검은 무효화된다. 실제 등록 실행은 `INPUT_ONLY`, `executable=false`로 유지한다.
- 운영 주소 조회는 출고 4개·반품 3개를 확인했다. 주소·전화번호·계정 키는 검증 증거에서 제외했다.
- 상품 `220114OC012`(1229, 11번가 4088296117)의 목표 300개 미접수 검토 `da4bb6ad-50bf-445a-9cf6-42b8c260f981`을 준비했다. 이 검토 API는 현재 마켓 수량을 조회하지 않는다. 별도 읽기 조사에서는 실제 재고 997개를 확인했으며, 검토 화면의 `observedQuantity=null`을 997로 바꾸거나 쓰기 성공으로 표시하지 않는다.
- 운영 상품 필드 해시 불변, 전체 필드/등록/재고 쓰기 카운터 각각 0을 확인했다. 실제 수량 PUT은 아직 실행하지 않았다. 다음 실제 적용은 사용자가 검토한 상품·목표 수량에 한하며 실행 직전 현재 상태를 재검사한다.

배포 전 추가 DB 백업 10,533,503 bytes, SHA-256 `6f4d0380ccee6549d1a0945202716ca1b105c02d53006d37ab89af8ed03eee08`, pg_restore 목록 검증 완료. 과거 백업과 롤백 이미지는 보존했다.

[배포 증거](evidence/2026-09-08-elevenst-followup-deployment.json), [운영 API](evidence/2026-09-08-elevenst-followup-live.json), [범위별 검사](evidence/2026-09-08-elevenst-followup-tests.json). 수량 103건·등록 입력 77건·API 379건·워커 79건, 프론트 타입/빌드 및 수량 8·등록 입력 7개 브라우저 fixture 통과. 이는 이번 범위 검사이며 전체 백엔드를 새로 모두 실행했다는 뜻은 아니다.

판매중 상품이 적은 원인 및 캡처 코드 14개 중 SB 삭제 상태 13개의 교차 조사 요청은 [후속 백로그](2026-09-08-elevenst-sales-status-backlog.md)에 보존했다. 원인을 조사하거나 상품을 복원하지 않았다.

전체 완료율은 기존 고정 기준 약 89%를 유지한다. 이번의 양수 수량 지원과 등록 입력 준비를 전체 11번가 수량/등록 완료로 올리지 않는다. 남은 실제 선택 적용·11번가 0개/복구와 신규 등록 readback·마켓플러스 전송은 [남은 작업](2026-09-08-products-delivery-remaining.md)에 계속 표시한다.

운영 Chrome 2개 흐름도 통과했다. 기존 수량 검토에서 목표 300·현재 마켓 수량 미확인·미접수를 확인했고, 등록 입력은 출고/반품 주소 자동 선택 없이 실행 불가 상태였다. 새 검토/반영/입력 점검 POST 없이 상품 revision 0과 쓰기 0을 보존했다. 원래 MP 탭·URL·20행·수집 설정을 복원했다. [운영 Chrome 증거](evidence/2026-09-08-elevenst-operating-browser.json), [수량 화면](evidence/2026-09-08-elevenst-operating-stock-preview.png), [등록 입력 화면](evidence/2026-09-08-elevenst-operating-input-only.png).
