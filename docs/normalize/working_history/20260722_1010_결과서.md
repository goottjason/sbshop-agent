# 정상화 결과서 — 2026-07-22 10:10 (Phase B: 11번가 클레임 감지 정밀화)

## 트리거
"Phase B 가자" — D-098(전 마켓 정산0) 후속, 마켓별 취소/반품/교환 상태 **감지** 보강.

## Phase B 라이브 조사 결론 (그림 대폭 축소)
라이브 API 조사로 스토어·Cafe24는 이미 작동함을 확인 → 진짜 잔여 공백은 11번가뿐이었다.

| 마켓 | 취소/반품/교환 감지 | 근거(라이브) |
|------|------|------|
| **스마트스토어** | ✅ 이미 처리 | 매퍼가 CANCELED/RETURNED/EXCHANGED 매핑 + 취소 라이브작동(정가영·이명동). 네이버는 완료클레임을 productOrderStatus로 반영. 코드 불필요 |
| **Cafe24**(G마켓/옥션) | ✅ 이미 처리 | 반품 R*→RETURNED 라이브작동(곽금희) |
| **11번가** | ❌ 유일 공백 → D-099로 처리 | 클레임 목록 API 부재(7개 후보 전부 -997, 정상 엔드포인트 200 대조검증). 단건 orderlistalladdr엔 ordPrdStat/ordPrdStatNm 실재 |

## 처리 결함
- **D-099 (P2, 표준)** — 11번가 클레임 감지 정밀화. **검증통과(단위+스모크)**, 라이브 E2E는 실 클레임 대기.

## 핵심 결정과 이유
- **스토어/Cafe24 무변경 결정**: 라이브로 이미 작동 확인 → 불필요한 수정 배격(회귀 리스크 회피). D-098로 정산0까지 이미 완료.
- **11번가: absence→blind CANCELED를 상세조회 실상태 판정으로 교체**: 클레임 목록 API가 없다는 라이브 확정 위에서, 유일 경로인 orderlistalladdr의 ordPrdStatNm(취소/반품/교환)으로 정밀 분류. 클레임 아니면(구매확정 등 aged-out) 상태 불변 → 기존 오취소 결함까지 해소.
- **상태명 부분일치 매핑**: ordPrdStat 숫자코드는 문서마다 상이(2010 vs 901)해 신뢰 낮음 → ordPrdStatNm 상태명(취소/반품/교환) 부분일치가 코드계 무관하게 견고.
- **D-031 정정**: "상세조회에 상태 필드 없음" 결론은 오류(파서가 ordPrdStat를 안 읽었을 뿐). 라이브 응답에 실재 확인.

## 구현 (커밋 5658431, TDD)
- `ElevenstStatusMapper.mapClaimStatus(ordPrdStat, ordPrdStatNm)` — 상태명 부분일치, 클레임 아니면 null.
- `ElevenstOrderAdapter.resolveClaimStatus(apiKey, ordNo)` — 단건 상세조회→매핑.
- `ElevenstOrderSyncService.detectClaims`(구 detectCancellations) — 사라진 non-terminal 주문 실상태 판정, 클레임만 반영, 아니면 불변. D-098 정산0 정규화가 이어짐.
- 테스트: 매퍼 5건 + 서비스 D-099 4건. 회귀 core+infra+api 전체 통과.

## 판정·게이트
- 회귀 전체 통과. 표준 등급.
- **스모크 검증**: 배포(00:44:34Z) 후 11번가 동기화 → ELEVEN_STREET_SYNC SUCCESS(새 상세조회 경로 무결). 현 DB 클레임 0건이라 상태변경 없음(정상).
- **검증 한계 명시**: 11번가 클레임 주문이 DB에 없어 라이브 E2E 불가. 단위테스트로 로직 고정. 실 클레임 발생 시 RETURNED/CANCELED 전환+정산0을 라이브 확인 필요.

## Phase B 종결 상태
- 취소/반품/교환 **감지**: 스토어·Cafe24(기존 작동 확인) + 11번가(D-099 신규) → 전 마켓 커버.
- 취소/반품 **정산0**: 전 마켓(D-098) 완료.
- 쿠팡 반품 전방감지(D-097) + 전 마켓 정산0(D-098) + 11번가 클레임 감지(D-099) = 취소/반품/교환 확장 일단락.

## 다음 단계 참조
- **11번가 라이브 E2E 대기**: 실 11번가 취소/반품/교환 주문이 생기면 orderlistalladdr ordPrdStatNm 실제 문자열(예: "취소완료"·"반품완료" 정확 표기)과 detectClaims 전환을 라이브 확인. 상태명이 예상과 다르면 mapClaimStatus 부분일치 키워드 조정.
- **11번가 absence 전제**: "클레임 주문이 4개 진행목록에서 사라진다"는 전제는 여전히 미검증(실 클레임으로 확인 필요). 만약 반품이 dlvcompleted에 잔류하면 detectClaims가 트리거 안 될 수 있음 → 그 경우 terminal 주문도 주기적 상세조회 재확인 검토.
- **교환 정산**: EXCHANGED는 정산0 제외(결제유지). 교환 추가배송비/차액은 별도 판단 후보.
- 원장 `발견` 상태 잔여 결함 확인 후 다음 배치 판단.
