# 정상화 사이클 15 결과서 — 2026-07-11 01:32

> 사용자 요청: 푸시 전 모든 메뉴·버튼·액션 전수 QA 체크리스트 작성 → 결함 수정 → 푸시. 5개 도메인 조사 에이전트 병렬 투입 후 리더 종합.

## 산출물
- **전수 체크리스트**: `docs/normalize/qa-checklist-20260711.md` (대시보드·OrderGrid 22액션·이메일자동추적·마켓전파 매트릭스·기타 페이지·죽은코드 인벤토리)

## 처리 결함

| ID | 요약 | 심각도 | 등급 | 상태 |
|----|------|--------|------|------|
| D-058 | 배송정보 미입력 시 택배사 'ETC' 오표기 | P2 | 경량 | 검증통과(수정) |
| D-059 | 대시보드 지표 하드코딩 0 → 실데이터+확장 | P2 | 표준 | 검증통과(수정) |
| D-060 | 가격/재고 수정이 마켓에 미동기(호출경로 부재) | P1 | 중대 | 발견(보고·요승인) |
| D-061 | 배송 마켓 전파 갭(수정 미전파, ESM+/카페24 미구현) | P1~P2 | 중대 | 발견(보고·요승인) |

## 사용자 질문별 답변

1. **대시보드 지표** → 전부 하드코딩 0이었음. **수정**: 실집계 연동 + 지표 7개로 확장(전체·미발주·구매준비·배송진행중·배송완료·통관오류대기·배송처리대기) + 새로고침. '통관 오류/대기'용 백엔드 통관상태 필터 신설.
2. **미입력 배송정보 ETC** → 사용자 지적이 정확. 그리드 렌더는 정상이나 `fromMarketCode("")`가 ETC로 매핑돼 저장된 게 원인. **수정**: 빈/공백→null(빈칸).
3. **iHerb 이메일 자동 배송추적** → 체인 5링크 모두 배선·**스케줄러 실제 가동** 확인(코드 결함 없음). 전제: IMAP 자격증명 설정 + 실 발송메일. (초기 조사 FAIL 오판을 코드 재확인으로 정정.)
4. **배송정보 수정 마켓 반영** → 배송"처리"·이메일 경로는 쿠팡·스토어·11번가 전파. 배송정보 "수정"은 미전파, ESM+/카페24 미구현(D-061, 중대).
5. **가격/재고 마켓 동기화** → **동기화 안 됨**(호출 경로 자체 부재, D-060). 스토어·11번가는 클라이언트 구현됐으나 호출되지 않고, 쿠팡·카페24는 클라이언트도 로컬 Map만. → 중대 등급으로 승인 후 별도 처리 권고.

## 핵심 결정과 이유
- **중대(마켓 API 계약) 갭은 임의 수정하지 않고 보고**: D-060/D-061은 실 마켓 API 호출을 신설/수정해야 해 라이브 검증·사용자 승인이 필수(스킬 게이트: 중대는 자율마커로도 생략 불가). 푸시 전 무단 통합은 위험 → 체크리스트·원장에 명확히 표면화하고 결정 요청.
- **안전 결함(경량·표준)만 수정 후 푸시**: D-058·D-059는 마켓 계약 무관, 게이트 통과분만 반영.

## 게이트
- 백엔드: `:core:test`(신규 `ShippingCarrierTest` PASS), `:infrastructure:test`·`:api:test` **BUILD SUCCESSFUL**(통관필터 포함). 유일 실패 `SmartStoreOrderFetchFailureTest`는 **기존 flaky**(clean HEAD 스태시 후 동일 실패 확인 — 날짜 민감성, 변경과 코드경로 무관).
- 프론트: `tsc -p tsconfig.app.json` clean + `npm run build` EXIT 0.

## 미해결·보류
- **D-060(P1, 중대)**: 가격/재고 → 마켓 동기화. 사용자 명시 기대치 → 최우선 다음 배치 후보(승인 필요). 마켓별 syncPriceAndStock 호출 경로 신설 + 쿠팡/카페24 실 API 구현.
- **D-061(중대)**: ESM+/카페24 배송 어댑터, updateShippingInfo 마켓 전파.
- **P3**: ProductRegisterPage 대량저장 필드 누락(데이터 완결성), 프론트 죽은코드 정리(productApi 미사용 함수 등).
- 기존 열린: D-045(ESM+ 라이브), D-052(G마켓/옥션 데이터 임포트), 기존 flaky 테스트(SmartStore fetch — 날짜 고정 리팩토링 권고).

## 다음 단계 참조
- **푸시**: 이 사이클 커밋 + 이전 미푸시(D-057) 함께 푸시 → 자동배포.
- **배포 후 확인**: 대시보드 7지표 실값/새로고침, 통합주문 미배송 택배사 빈칸(신규 동기화분), (기존 ETC 데이터는 잔존 — 필요 시 일괄 UPDATE 결정).
- **승인 대기 결정**: D-060(가격재고 마켓 동기)부터 진행할지 사용자 확인. 진행 시 중대 등급 → 라이브 검증 계획 포함해 별도 배치.
