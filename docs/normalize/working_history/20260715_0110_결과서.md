# 정상화 결과서 — 2026-07-15 01:10 · SP Phase 4 완료 (b·c·d) + 배포

## Phase 4 마무리 (P4b·c·d)

| 배치 | 처리 | 커밋 |
|------|------|------|
| P4b (F-MISC-8·9·10) | ProductSync 원시 new Thread → 관리 @Async + ActionLog 실패기록, 대상선정 서비스 이동, 응답메시지 정정 | `bbf0e1c` |
| P4c (F-BATCH-2) | 재시작 시 고아 PENDING→FAILED 부팅 복구(ApplicationReadyEvent) | `03ea176` |
| P4d (F-MISC-16) | SSE cross-JVM 브리지(Postgres LISTEN/NOTIFY): worker pg_notify→api LISTEN→로컬 재발행→기존 SSE | `e58e218` |

- **SP-1(비동기 예외 은폐) 완료**: sync(P4a)·ProductSync(P4b)·batch복구(P4c).
- **SP-2(JVM 로컬 상태) 완료**: sync 상태 DB화(P4a) + SSE 브리지(P4d).
- TDD·전 모듈 회귀 통과. P4b·c 표준(자율), P4a·d 중대(승인).

## 배포 (2026-07-15 push)
사용자 지시로 **P1~P4d 전 변경 main push = 자동배포**. 유의:
- **ddl-auto=update 첫 적용**: 기동 시 Hibernate가 전 엔티티를 운영 스키마와 대조 — `sb_market_sync_status` 자동 생성. 첫 기동 로그 확인 권장.
- **도는 배치 중단 가능**([[deploy-interrupts-running-batch]]) — push=api 재시작.
- 200→400 전환(P2·P3), /ship 응답 스키마 변경(P3), 주문삭제 엔드포인트 제거(P2) 라이브 반영.

## 라이브 검증 필요 (단위테스트 밖)
- **P4d SSE 왕복**: worker 정기 동기화 완료 → 브라우저 SYNC_COMPLETED/FAILED 1회(이중 아님); 커넥션 끊김 후 ~5s 재연결; 전용 커넥션 홀드의 풀 영향; 종료 정리.
- **P4a sync 상태 UI**: /sync/status가 이제 값 표시(worker 갱신분 api서 조회).
- **P2 프론트 400 메시지 표시**: 종료상태 송장수정·비NEW 취소 등.

## 다음 단계 참조 (P5 이후)
- **P5 구조 리팩토링(SP-9·11)**: 서비스 계층 추출(Supplier·MarketRegistration — ProductSync는 P4b서 일부 이동), 중복 제거(batch 트리거 4종·이미지 3경로·cancel/confirm batch·마켓 sync 골격). 동작 불변 리팩토링 → 회귀 게이트 보증, 표준.
- **P5b 보안(축소)**: 시크릿 응답 마스킹(F-CRED-1·7)·무인증/internal 접근제어(F-MISC-17·7)만.
- **P6 응답 DTO(SP-5)**: 도메인 엔티티 노출 제거, 프론트 계약 병행.
- **SP-3 상품 후속**: F-PSRC-2·6, F-PROD-12·16(이미지·bulk 부분실패), P4b서 남긴 상품별 크롤예외 표면화.
- **잔여 개별 결함**: F-SYNC-19(customs 동기 sleep), F-BATCH-M1(index 정렬 오적용, 🔴), F-BATCH-ST1(status OOM), F-PSRC-14(publish 후 저장실패 고아, 🔴), F-SUP-UC-1(환율 덮어쓰기, 🔴), F-MISC-18(email 중복처리, 🔴) 등.
