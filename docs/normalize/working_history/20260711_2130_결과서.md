# 사이클 23 결과서 — 상품 액션 상세로그 + 소스이미지 크롤 무반응 (2026-07-11 21:30)

## 처리 개요
사용자 신고 2건: (1) 가격/재고 수정 활동로그가 "성공"만 뜸 → 마켓별 상세(DB/쿠팡 번호 성공, 스마트스토어 번호 실패(사유) 등) 원함, (2) 소스이미지 크롤 버튼 무반응 + 진행현황 미표시. 1 scout 진단 → D-077·D-078 수정·커밋(d8c7b74).

| 결함 | 내용 | 상태 |
|------|------|------|
| D-077 | 가격/재고·이미지 활동로그 마켓별 상세(상품번호·실패사유) | 검증통과 |
| D-078 | 소스이미지 크롤 버튼 무반응 해소 + 활동로그 배선 | 검증통과 |

## 근본원인·수정
- **D-077**: `ProductController.updatePriceStock`이 `MarketRepublishResult`(synced/skipped/failed)를 받고도 고정 메시지만 기록. → `buildMarketResultMessage` 헬퍼로 "DB 저장 완료 | 쿠팡 7283748383 성공, 스마트스토어 2939395 실패(401 Unauthorized), G마켓 스킵" 조립. marketItemId는 `marketRegistrationRepository.findByProductId`+`extractMarketCode`(방법 A, 최소변경, 레코드 무변경), 실패사유 50자 절단. uploadImages/uploadImagesByUrl 일관 적용.
- **D-078**: (프론트) antd `disabled` 버튼이 Tooltip도 안 떠 "먹통" → `disabled` 제거로 항상 클릭 가능, `handleCrawl`에서 비-iHerb warning·빈결과 warning·성공 토스트·loading 피드백. (백엔드) `crawlSourceImages`가 활동로그 미배선(D-076 누락) → `SOURCE_IMAGE_CRAWL` 상수 + record(수집 N개/빈결과/실패). ProcessStatusPage 한글 라벨.

## 설계 결정
- 마켓별 상세는 **방법 A**(컨트롤러에서 등록정보 조회) 채택 — MarketRepublishResult/프론트 토스트 타입 확장(방법 B)은 파급 커 후순위. 프론트 토스트는 이미 마켓별 성공/실패 표시 중(상품번호만 미포함).

## 판정·검증
- 리더 직접 게이트(세션 재시작 이후 팀 운영): :core:test·:api:test 코드테스트 통과(신규 ProductControllerActionLogDetailTest 5건 — 마켓상세·50자절단·이미지·크롤 성공/빈결과), 프론트 tsc -p 0·npm build ✓. FAILED 후 예외 재throw로 에러 응답 보존.
- **환경 제약**: 로컬 Docker 미실행으로 testcontainers 컨텍스트 스모크 3건 initializationError — cycle 무관(미변경), CI/서버 Docker 빌드에서 검증.
- 리더 보완: fixer 대상 외였던 ProcessStatusPage의 SOURCE_IMAGE_CRAWL 라벨 추가.

## 라이브 확인 필요 (배포 후)
1. iHerb 상품 가격/재고 수정 → 진행현황에 "DB 저장 완료 | {마켓} {상품번호} 성공/실패(사유)" 마켓별 상세가 뜨는지.
2. iHerb 상품 소스이미지 크롤 버튼 → 결과 토스트(N개/없음) + 진행현황에 "소스이미지 크롤" 기록. 비-iHerb 클릭 → warning 토스트.

## 다음 단계 참조
- 배포 후 위 2건 라이브 확인. 마켓별 상세가 부족하면 방법 B(MarketRepublishResult에 marketItemId 포함 + 프론트 토스트 상품번호)로 승격.
- 후속: 배치(B1~B4) 비동기 완료 SUCCESS/FAILED 기록, 활동로그 AOP 리팩토링(선택), D-070(11번가 parseOrderElement 정규화)·D-071(스마트스토어 취소/반품)·D-072(배송실패 UI). 사이클20 라이브 잔여(Cafe24 PCCC 필드명·이명동 상태코드·iHerb 재입고일)·D-075 Cafe24 재인증 확인·D-076 활동로그 실측.
- push/배포는 사용자 확인 후.
