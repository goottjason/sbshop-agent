# 사이클 8 결과서 — 2026-07-07 (D-020·배포·운영 정착·라이브 E2E)

## 최종 결과: 운영 서버 완전 가동 (병합 이후 최초)

라이브 E2E 전 항목 그린: orders(실데이터)·products(실데이터)·common/codes·suppliers·currencies·market-credentials 200, 프론트 200, api·worker 기동 안정.

## 이번 사이클에서 해소된 기동/기능 차단 (4~6번째 층)

| 결함 | 내용 | 처리 |
|------|------|------|
| D-020 (P1) | R2 자격증명 부재가 api·worker 부팅 차단 | @Lazy+ObjectProvider 지연 결합 (72a2c17) |
| D-019 (P0) | 운영 DB 27컬럼 aa_ 접두사 드리프트 → 첫 조회에서 크래시 루프 | 사용자 승인 후 수동 DDL 리네임 (데이터 무손실) |
| D-021 (P1) | @Lob이 PG에서 OID 해석 → 주문/상품 조회 전멸 | @Lob 제거 + testcontainers 재현 테스트 (df89c98) |
| 배포 스크립트 결함 | ~/projects에서 git pull 시도 | 경로 교정 (6a0b51a) |
| nginx IP 캐시 | 컨테이너 재생성 후 전 경로 502 | nginx reload (운영 절차 후보 기록) |

## 판정 요약

- D-020: 팀 검증(verifier-c8 PASS — 격리 워크트리 Red 재현). D-021: 경량 등급 리더 직접 TDD (운영 오류를 testcontainers로 자리수까지 재현 → Green). D-019: 사용자 승인 DDL.
- 교훈 실증: D-021은 **H2로 절대 못 잡는 결함** — 사이클 4에서 구축한 실 PostgreSQL 테스트 기반이 이번 진단·수정의 결정적 도구였음.

## 남은 사용자 조치 (기능 완전화)

1. **Cloudflare R2 키**: 서버 ~/projects/.env에 CLOUDFLARE_R2_* 추가 + docker-compose environment 전달 → 이미지 업로드 활성 (현재는 호출 시 명확한 에러).
2. **Cafe24 토큰 재발급**: refresh token 만료 (invalid_grant) — Cafe24 기능 사용 시 필요.
3. UI 수동 확인: 주문 그리드 표시·라인아이템 편집(소싱금액/물류비/유니패스)·배송 택배사 저장.

## 다음 단계 참조

- 배포 절차에 nginx reload 포함 (deploy-sbshop.sh 개선 후보).
- 잔여 소소: 프론트 lint 6건, api AsyncConfig 껍데기, onError 토스트, 국제 택배사 enum, 자격증명 API 마스킹(후보).
- 원장 최신: D-001~D-021 중 미해결은 위 후보들뿐. 주요 기능 차단 결함은 전부 해소.
