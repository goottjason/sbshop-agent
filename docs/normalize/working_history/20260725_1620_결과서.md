# 정상화 결과서 — 2026-07-25 16:20 (D-107 11번가 배송중 수취인 소실 긴급수정)

## 처리 결함
- **D-107** 11번가 배송중 주문 동기화가 수취인 이름·주소를 지워 "-"로 표시 — **수정완료(검증통과)**

## 근본원인
`ElevenstOrderAdapter.parseShippingElement`(배송중 목록 파서)가 `recipientName`·`address`·`message`·`ordererName`을 빈 문자열 `""`로 하드코딩. `Order.update`는 `recipientName`을 `!= null`로만 가드하므로 `""`가 기존 실이름을 덮어씀.
- `recipientPhone`은 `isUsablePhone`(blank 거부), `address`는 `protectAddress`(D-074)로 보호돼 살아남음 → 스크린샷의 "전화·주소 있고 이름만 '-'" 패턴과 정확히 일치.
- D-066에서 통관번호는 이미 `emptyToNull`로 이 함정을 회피했으나 이름/주소는 방치돼 있었음.

## 수정 내용
- `parseShippingElement`가 배송중 목록의 수취인/구매자 필드(`rcvrNm`, `rcvrPrtblNo`, `rcvrMailNo`, `rcvrBaseAddr`+`rcvrDtlsAddr`, `ordDlvReqCont`, `ordNm`, `ordPrtblTel`)를 파싱.
- 태그 부재 시 `emptyToNull`로 null 정규화 → `Order.update` null-guard가 기존 값 보존(통관번호와 동일 정책).
- 태그 존재 시 파싱해 **복원**. `marketProductCode`/`productName`도 `""`→null.

## 판정 요약
- **TDD**: Red(2건 실패, ""반환) → Green(파싱+emptyToNull).
- **신규 테스트**: `ElevenstShippingRecipientPreservationTest` 2건 — (a)rcvrNm 부재→recipientName·address null 보존, (b)rcvrNm·주소 존재→복원. PASS.
- **회귀 게이트**: `:core:test` 전체 PASS. D-066 배송중 통관번호 테스트 회귀 PASS.
- **영향 범위**: core 1파일 private 파서 내부 수정, 시그니처 불변 → worker/api·FE 무변경.
- **리스크 등급**: 표준 (마켓 API 매핑 계약, core 1파일).

## 데이터 복원 경로
이미 `""`로 오염된 기존 배송중 주문은:
1. 다음 동기화 시 배송중 목록의 `rcvrNm`으로 자동 복원(목록이 이름을 담고 있을 경우), 또는
2. 배송완료 전환 시 `dlvcompleted` 경로(`parseOrderElement`)가 복원.
별도 백필 배치 불필요 — 정상 동기화 흐름으로 자연 복원.

## 2차 수정 — 라이브 진단 후 실복원 (2026-07-25 17:0x, 커밋 63c839d)
1차 수정(커밋 2bffdda) 배포 후 재동기화에도 이름이 `-` 그대로여서 서버 라이브 진단:
- **DB 확인**: 두 주문 `recipient_name`=`""`(길이0), **주소·전화는 정상 존재**(주소는 protectAddress로 보존). 실제 유실은 **이름만**.
- **동기화 로그(07:54, 배포 후)**: "11번가 배송중 조회 완료: 2건" → 두 주문 SHIPPED로 매칭됐으나 이름 여전히 `""` → **배송중 목록 API가 `rcvrNm`을 아예 주지 않음** 확정.
- **결론**: 1차 수정은 "추가 손실 방지"만 함(list에 rcvrNm이 없어 복원 불가). 이미 오염된 이름을 되살리려면 상세조회 필요.

**2차 수정**: `fetchOrders` 배송중 루프에서 `recipientName==null`이면 `enrichRecipientFromDetail` 호출 →
단건 상세조회(`claimservice/orderlistalladdr`, `rcvrNm 포함`)로 수취인/구매자 필드를 in-place 복원.
배송상태(SHIPPED)·송장은 배송중 값 유지, 상세조회도 비면 null 유지(기존 값 보존). 상세 호출당 300ms sleep(rate).
원 설계 주석 "개별 조회로 폴백" 실구현. 신규 테스트 2건 추가, `:core:test` 전체 PASS.

## 3차 — 전 마켓 확장 방어 (D-108, 2026-07-25, 사용자 요청)
사용자: "11번가 외 다른 마켓도 배송중·배송완료 시 개인정보 미반환으로 유실되지 않도록 전수 검토·방어."
- **아키텍처 발견**: 4개 마켓 sync(쿠팡·스마트스토어·11번가·Cafe24)가 **모두 단일 `Order.update`(9-arg)를 경유** → 중앙 한 지점 방어 가능. (수동 편집은 `updateAddress` 등 별도 경로라 무영향, F-ORD-23 클리어 유지.)
- **가드 신설**: `isMeaningfulPii`(null·blank·'*' 마스킹 거부)를 recipientName/zipcode/address/ordererName에 적용. 메시지는 자유텍스트라 blank만 거부. 전화는 기존 `isUsablePhone` 유지. 마스킹("정*영")·빈값이 실값을 덮지 못함.
- **검증**: `OrderSyncMergeGuardTest` 4건 Red→Green, `:core:test` 전체 PASS, api·worker 컴파일 PASS. **전 마켓 DB 감사** — empty/masked name·addr 전 마켓 0 → 오염 없음, 순수 예방. 11번가 라이브 복원(정채영·이선) 확정.

## 다음 단계 참조
- **커밋됨, push 대기** — main push 시 자동배포=api 재시작. 배포 후 라이브 확인: 11번가 주문 동기화 1회 실행 → 배송중 11번가 주문(20260722086940464·20260721086527055)의 수취인 이름이 그리드에 복원되는지 확인.
- **미확인 가정**: 11번가 배송중 목록(`/rest/ordservices/shipping/`) 응답이 실제로 `rcvrNm`을 담는지는 라이브 응답으로만 최종 확정 가능. 담지 않아도 이름은 **소실되지 않음**(null 보존) — 최악의 경우 배송완료 전환 시 복원. 배포 후 첫 동기화 로그/그리드로 검증 권장.
- 잔여 백로그: 기존과 동일(D-093/094 가격정책, FE 러너 부재 등).
