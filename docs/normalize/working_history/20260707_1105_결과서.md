# 사이클 1 결과서 — 2026-07-07

## 처리 결함 (5건 전부 검증통과 → 커밋)

| 결함 | 심각도/등급 | 처리 | 커밋 |
|------|------------|------|------|
| D-001 | P0/중대 | ElevenstRestClient 빈 이름 분리 (elevenstOrderRestClient/elevenstMarketRestClient) | 80cc384 |
| D-003 | P2/표준 | productBatchExecutor @Bean 등록 | 32bbe1b |
| D-009 | P0/중대 | api AsyncConfig에 @Configuration("apiAsyncConfig") — core와 충돌 해소 | 32bbe1b |
| D-006 | P2/표준 | SmartStore 탐색 테스트 삭제 (하드코딩 인증정보) | 2c36ff3 |
| D-012 | P3/경량 | 쿠팡 탐색 테스트 2건 삭제 | 062cab7 |

## 판정 요약

- 내부 QA(qa-verifier) 5/5 PASS. 판정서: `_workspace/verify/` (휘발 — 요지는 본 문서로 자기완결)
- 리더 회귀 게이트: `./gradlew test --rerun-tasks` 전체 그린 (core 19 + api 3, 실패 0). 프론트엔드 변경 없음.
- 사용자 승인: 2026-07-07 "자율로 진행해" — 중대 등급 포함 사이클 1 배치 승인 + 자율 모드 전환(`_workspace/.autonomous`). push는 계속 수동.

## 핵심 결정과 이유

1. **D-001 삭제 대신 빈 이름 분리**: 구버전(GET/EUC-KR XML)·신버전 모두 실사용처 존재 — 삭제 시 기능 손실. 클래스 통합은 D-010(구조 후보)으로 분리.
2. **D-009·D-012 배치 확장 승인**: D-001 해소가 드러낸 2차 P0(D-009)와, 게이트를 막던 탐색 테스트(D-012)를 편입 (배치 5건 — 규칙 상한 내). D-003/D-009는 같은 파일·비겹침 라인이라 단일 커밋으로 묶음(둘 다 행위 변경).
3. **D-002 오판 확정**: OrderSyncScheduler는 실제 활성 (@Scheduled 정상, TODO는 잔여 주석). 초기 탐사의 오독 — 원장 보류 처리.
4. **탐색 테스트 삭제로 가려진 인프라 이슈를 D-013으로 명시 기록** — 침묵 매장 방지.

## 미해결·보류

- 보류: D-002(오판)
- 미해결 7건: D-004(이미지 다운로더 통합), D-005(after-migrate↔Flyway, **중대**), D-007(lint 59), D-008(미호출 API 함수), D-010(Elevenst 클래스명), D-011(bare @Async — 전용 풀 미배선), D-013(api 테스트 datasource 미구성)

## 수동 조치 필요 (사용자)

- **네이버 커머스 API 키 회전**: D-006으로 삭제한 clientId/clientSecret이 git 히스토리에 잔존. 사용자 판단: "보안이 중요한 프로젝트 아님" — 회전 보류 결정 (2026-07-07). 공개 레포 전환 시 반드시 회전.

## 다음 단계 참조

- **사이클 2 배치(구조 변경 배치)**: D-004 + D-010 — 이미지 다운로더 3벌 통합, Elevenst 클래스 리네임. 전후 테스트로 동작 불변 증명 필수 (Tidy First — 행위 변경과 분리).
- **사이클 3 후보(행위 배치)**: D-011(전용 풀 배선 — D-003 후속) + D-013(api 테스트 datasource 구성 — H2 격리 config 우선 검토, testcontainers 도입은 신규 의존성이라 사용자 승인 필요).
- D-005는 중대(스키마) — 자율 모드에서도 사용자 승인 관문 필수. 운영 DB의 실제 스키마 상태 확인이 선행돼야 함 (after-migrate.sql이 이미 적용된 DB와 신규 DB의 상태 분기 주의).
- api 모듈에 @SpringBootTest 실기동 검증이 현재 0 — D-013 해소 전까지 "운영 컨텍스트 실부팅"은 이 하네스에서 미검증 상태임을 인지할 것.
- 판단 연속성: 진단 산출물은 `docs/normalize/codebase-map.md`(지도)·`defect-ledger.md`(원장)가 최신 상태.
