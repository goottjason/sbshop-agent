# 2026-08-08 13:09 정상화 사이클 결과서 — D-146 (11번가 상태 잠금 영구 거부 분류)

## 처리 결함

| 결함 | 내용 | 등급 | 판정 |
|------|------|------|------|
| D-146 | 11번가 "이미 배송중" 거부가 일시 실패로 분류돼 30분마다 무한 재시도 | 경량 | **통과** |

## 0단계 — 코드 수정 전 확인 (이번 사이클의 핵심)

계획서가 요구한 대로 **"11번가에 송장 수정 API가 있는가"를 먼저 확정**했다. 없다고 단정하고 영구 거부로
분류하면 있는 기능을 버리고 사람에게 수동 작업을 떠넘기는 셈이기 때문이다(네이버 D-145에서 세운 규율).

**결론: 11번가에 송장 수정 API는 없다.** 3중 확증:

1. **게이트웨이 전수 조회** — 11번가 API 게이트웨이는 키 없이 호출해도 등록 경로는 `-100`(키 누락),
   미등록 경로는 `-997`(등록된 API 정보 없음)을 돌려준다. **아무것도 실행하지 않고 엔드포인트 존재
   여부만 조사할 수 있는 채널**이다. 12개 서비스 그룹 × 1,150여 경로 패턴
   (`deliverychange`·`modifyinvoice`·`reqdeliveryedit`·`dlvmodify` 등)을 조회했고 **수정 계열 0건**.
2. **외부 구현체 대조** — GitHub 5개 저장소(PHP·Ruby·Python·Laravel)가 실제로 쓰는 11번가 경로 25개에도
   수정 계열이 없다.
3. **라이브 시험** — 발송처리 재호출은 5·8-파라미터 모두 `-3313 해당 배송번호의 주문상태가 이미 변경
   되었습니다`(배송중·구매확정).

### 부산물: `reqdelivery`의 미지 9-파라미터 변형

전수 조회로 `reqdelivery`가 **5·8·9 파라미터** 세 형태로 등록된 것을 발견했다(우리는 5·8만 사용).
자리별 값을 바꿔가며 에러 코드를 역추적한 결과 **6번 자리가 "추가 송장번호"**였다:

| 6번 자리 값 | 응답 |
|---|---|
| 순수 숫자(송장번호 꼴) | `-3306` **추가 송장번호**가 유효하지 않습니다 |
| `Y`/`N`/기타 | `-3307` 상품수량을 입력해 주세요 |

즉 이 변형은 한 상품주문을 여러 박스로 나눠 보내는 **분할발송(송장 덧붙이기)**용이지, 기존 송장을
**대체**하는 기능이 아니다. 실배송번호로 호출해도 `-3306`에서 막혀 상태 게이트에 닿지도 못한다.

**라이브 시험 중 마켓 데이터는 변경되지 않았다** — 파라미터 검증이 배송번호 조회보다 먼저 일어나
요청이 주문에 닿기 전에 멈췄고, 읽기 전용 조회로 확인했다(li231 `invcNo` 363082001112 · `ordPrdStat` 401 그대로).

## 수정 내용

- `MarketplaceShippingService.isNonRetryableMarketState`에 `"주문상태가 이미 변경 되었습니다"` 추가.
  상태 값(배송중·구매확정 등)에 의존하지 않도록 **앞부분 문구만** 본다.
- **D-128 구분 유지** — `"존재하지 않는 배송번호"`는 마켓 상태 잠금이 아니라 우리 요청 오류이므로
  재시도 대상으로 남긴다. 과거 이 오분류가 거짓 종결을 만든 이력이 있어 기존 회귀 테스트를 존치했다.
- 테스트 3건 추가(`MarketplaceShippingTerminalTest`): 배송중 · 구매확정 · 원인 체인 래핑(D-150 경로).

## 게이트

- Red 확인: 신규 3건만 실패(기존 8건 통과) → Green: 11/11
- 전체 회귀: **982건 · 실패 0 · 오류 0** (`BUILD SUCCESSFUL in 4m 13s`)
- 프론트 변경 없음 → 프론트 게이트 해당 없음
- 리스크 등급 경량 + `_workspace/.autonomous` → 커밋 자동 통과, **push는 승인 대기**

## 라이브 검증 결과 (2026-08-08 04:22~04:34 UTC) — **통과**

1. **배포 확인** — 서버 커밋 `7e53070` · 컨테이너 재생성 `2026-08-08 04:22:23 UTC`(커밋 이후) ·
   **주문조회 `HTTP 200`**(`/api/v1/orders`, 0.37s). 기동 로그만으로 끝내지 않았다.
2. **1차 이메일 페치** — 11번가 **8건**이 `마켓(ELEVEN_STREET) 상태 잠금 - 전송 종결`로 분류됐다.
   대상 6건 + 구매확정 1건(li197) + li233. 상태 값이 배송중/구매확정 둘 다 잡혔다(문구 기반 판정이 의도대로 동작).
3. **DB** — 6건 전부 `manual_fix_required = t` · `tracking_sent_to_market = t`
   (li 198·216·221·225·230·231, 여기에 197·233 추가 → 11번가 수동수정 8건).
4. **재시도 중단 확증** — 2차 페치 실행 후 로그에서 `11번가 발송처리 실패` **0건**.
   30분마다 반복되던 루프가 실제로 멈췄다.

11번가 잔여 4건(li 145·154·204·234)은 `market_tracking_no`가 비어 있는 별개 상황(마켓 값 미확보)이며
D-146 대상이 아니다.

## 기대 결과 (배포 전 작성)

6건이 `⚠ 마켓 전송 대기` → `🔒 마켓 수동수정`으로 바뀌고 재시도가 멈춘다.
배지 분포: 수동수정 5 → **11** · 전송대기 6 → **0** · 반영됨 44.

검증 순서(계획서 3단계 그대로):
1. 배포 확인 — 서버 커밋 해시 + **주문조회 200까지**(기동 로그만으로 끝내지 말 것, 2026-08-07 4분 장애)
2. `POST /internal/email/fetch` 1회 → 로그에서 11번가 건에 `전송 종결` 문구
3. DB: `manual_fix_required`가 6건에 서는지
4. 배지 분포 재확인

## 사용자 조치 대기 — 11번가 셀러오피스 수동 송장 수정 (6건)

API 경로가 없으므로 사람이 직접 고쳐야 한다. DB로 재확인한 값이다:

| li | 주문번호 | 넣을 송장(진짜) | 지금 마켓 값 | 택배사 |
|----|---------|----------------|-------------|--------|
| 231 | 20260801088987161 | 315399499984 | 363082001112 | 롯데 |
| 230 | 20260801088977098 | 315399492520 | 363082000865 | 롯데 |
| 225 | 20260730088728533 | 424438282564 | 6063465794604 | CJ |
| 221 | 20260729088470612 | 424438282612 | 6079990333509 | CJ |
| 216 | 20260726087776259 | 424438055145 | 424410280092 | CJ |
| 198 | 20260721086527055 | 424437615335 | 6079990378097 | CJ |

고치면 다음 동기화가 두 송장 값을 비교해 배지를 스스로 끈다(D-148/D-149 규율).

## 다음 단계 참조

- **D-146 라이브 검증**: 위 4단계. push 승인 후 진행.
- **G마켓 2건(li 402·454)** `Cafe24 API POST 호출 실패` 원인 규명 — 택배사 등록과 무관한 별개 오류. 미착수.
- **6단계 잔여**: 라인아이템 미러 컬럼 소비처 이관(`20260807_1940_결과서.md` 참조).
- **11번가 조사 자산**: 게이트웨이 `-997`/`-100` 채널은 앞으로 11번가에서 새 API를 찾을 때 문서보다 빠르다.
  파라미터 검증이 배송번호 조회보다 먼저라 **가짜 배송번호(`9999999999`)로 문법 실험을 해도 실주문은 안전**하다.
