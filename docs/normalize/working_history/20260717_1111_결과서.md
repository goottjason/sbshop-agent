# 정상화 결과서 — 2026-07-17 11:11 · 2차 API 재조사 + D-087 수정

두 갈래 작업: (1) 현재 코드베이스 기준 **API 흐름 2차 전면 재조사**(`docs/api-analysis-2nd/`) 생성, (2) 그 발견에서 승격한 **D-087(@Async 동기화/정산 ActionLog SUCCESS 오기록)** TDD 수정·회귀통과·문서반영. 미배포(로컬 커밋만, push 대기).

## 1. API 흐름 2차 재조사 (커밋 d009f9e)
- 1차(2026-07-14) 이후 R1~R6·재무버그·완전삭제 배포를 반영해 현 워킹트리를 처음부터 재진단. 1차 발견사항 비계승.
- **14 컨트롤러 · 56 엔드포인트**를 8섹션 형식(개요·호출체인·유스케이스·시퀀스·순서도·상태전이표·발견사항·테스트커버리지)으로 재작성. 168 mermaid.
- 16 도메인 유닛 병렬(Workflow) → 발견 **157건**: 🔴BUG 1 · 🟠GAP 48 · 🟡SMELL 47 · 🔵NOTE 61. `FINDINGS-CHECKLIST.md` 우선순위 집계.
- 도메인 밀도: order-sync 31 · order 27 · product 23 · batch 19 · misc 19.
- 드러난 시스템 패턴 2가지: ① @Async 동기화/정산 SUCCESS 오기록(→D-087) ② order 상태가드 공백(라인0건 공허참 통과, 종료/DELIVERED 편집·발송 미차단 — ORDA-4/ORDB-5/ORDC-1/4/7).

## 2. D-087 수정 (커밋 c4c7faa 코드 · ebce474 문서반영)
- **결함**: `OrderSyncController`가 `@Async` 동기화 서비스 호출 직후(즉시 반환) `record(SUCCESS)` → 실제 결과 무관 항상 SUCCESS. 정산(S5)은 이벤트 미발행이라 실패가 아예 안 남던 진짜 버그.
- **수정**: (a) S1~S4(쿠팡·스토어·11번가·G마켓) 컨트롤러 SUCCESS 제거 — STARTED·catch FAILED 유지, 완료는 기존 `SyncCompletedEvent`→`ActionLogSyncListener`에 위임(중복 제거). (b) S5 정산: `CoupangOrderSyncService`에 `ActionLogService` 주입, `syncCoupangSettlement` markCompleted/markFailed에서 `COUPANG_SETTLEMENT_SYNC` SUCCESS/FAILED 직접 기록(`recordSettlement`). (c) S6 통관 동기실행 불변.
- **TDD**: `OrderSyncControllerActionLogTest` 새 계약 재작성(트리거=STARTED만·SUCCESS never·동기예외 FAILED) — Red 5건 확인 → Green. 신규 `CoupangSettlementActionLogTest`(완료 SUCCESS·실패 FAILED·중복스킵 무기록) — Red 2건 → Green. 수동생성 테스트 2건 생성자 인자 보정.
- **게이트**: 전체 `./gradlew test` BUILD SUCCESSFUL(2m17s). 리스크=다도메인 관측성(표준~중대), 사용자가 명시 수정 지시로 승인.
- **문서반영(필수 요구)**: 5개 order-sync 2차 문서 §2/§4/§5/§7/§8 정정 + ✅해결 마커, FINDINGS-CHECKLIST 5건 [x].

## 미해결 · 보류
- **완전삭제 라이브 검증**(이전 세션 P1): 여전히 미완 — 사용자가 실제 삭제 실행 필요.
- **F-PSRC-9**(SB코드 동시생성 결번/중복): 2 JVM 공유 Postgres라 DB 레벨 조정 필요(advisory lock vs 유니크+재시도) — 전략 결정 대기.
- 2차 신규 백로그 152건(157−5) 미착수.

## 다음 단계 참조
1. **로컬 3커밋 push 여부 결정**(d009f9e·c4c7faa·ebce474). push=자동배포이므로 배치 PENDING 0 확인 후.
2. **2차 체크리스트 기반 백로그 착수**: 재현 가능한 🟠GAP 우선. 최임팩트 후보 — order 상태가드 공백 클러스터(ORDA-4/ORDB-5/ORDC-1/4/7, [[state-guard-case-by-case]] 정합), SYNCA-6(스토어 취소감지 부재).
3. F-PSRC-9 생성전략 결정 후 수정, 완전삭제 라이브 검증(사용자 액션).
- 워크플로 메모: 대규모 문서 재조사는 도메인 유닛 병렬(Workflow, 파일 비중첩)로 안정적. 생성자 시그니처 변경 시 수동생성 테스트 호출부까지 전 모듈 컴파일 필수([[compile-all-modules-before-merge]]).
