# 정상화 결과서 — 2026-07-22 09:05 (D-098 전 마켓 취소·반품 정산0 정규화)

## 트리거
사용자 요청: "다른 마켓도 반품/취소 등 상태 확장해줘" (쿠팡 D-097 후속).

## 진단 (3마켓 병렬 서브에이전트 + DB 라이브 실측)
쿠팡처럼 "일괄 이식"은 불가 — 마켓마다 메커니즘이 다름을 확인. DB 상태 분포 실측이 결정적이었다.

| 마켓 | 취소/반품 상태 감지 | 정산0 |
|------|------|------|
| **Cafe24**(G마켓/옥션/카페24) | ✅ 라이브 작동 — 곽금희 반품이 `R*`→RETURNED로 이미 저장됨(반품주문이 order_date 조회에 남고 매핑) | ❌ 26,611 유지 |
| **스마트스토어** | ⚠️ 취소만 작동(정가영·이명동 CANCELED). 반품/교환(claimStatus)은 어댑터가 안 읽음 | ❌ 78,140·47,590 유지 |
| **11번가** | ❌ 클레임 terminal 0건. 4개 진행상태만 조회, 클레임 조회 API가 코드·문서에 없음. 원본에 클레임정보 없음(dlvNo만) | ❌ (해당 없음) |

**핵심 재발견:** 서브에이전트가 우려한 "매핑 미검증"은 Cafe24·스토어 모두 라이브 작동으로 확인. **진짜 보편 공백은 정산0 미처리** — 취소/반품 3건이 정산액을 부풀린 채 남아 손익 왜곡(라이브 실측). 마켓 API 무관, DB 파생 가능한 안전한 수정.

**정정:** ESM+ Selenium 경로(원장 D-029)는 SP-E 리팩토링으로 폐기됨. G마켓/옥션/카페24는 전부 Cafe24 주문API 단일 경로.

## 처리 결함
- **D-098 (P1, 표준)** — 취소·반품 종결 lineItem 정산0 미처리(쿠팡 외 전 마켓). **검증통과.**

## 핵심 결정과 이유
- **정산0 정규화를 마켓 무관 단일 서비스로**: 상태 감지(마켓별 API 상이·불확실)와 정산 정정(DB 파생·확실)을 분리. 후자만 이번에 처리해 저위험·즉시가치·전 마켓 커버 달성.
- **교환(EXCHANGED) 제외**: 교환은 동일가 재발송으로 결제가 유지됨 → 정산0 대상 아님. `ShippingStatus.isRefundTerminal()`이 취소·반품만 true. 쿠팡 D-097도 반품만 0 처리했음(일관).
- **DB 전체 스캔 방식**: 30일 조회창에 의존하지 않고 마켓 전체 주문을 훑음 → 조회창 밖 과거 종결건(곽금희 06-23)도 교정. 멱등이라 매 동기화 자가치유.
- **verified=true 동반**: 정산동기화가 다시 덮지 않도록 잠금 신호.

## 구현 (커밋 7cf213e, TDD)
- `ShippingStatus.isRefundTerminal()` — 취소·반품=true, 교환=false.
- `TerminalSettlementService.zeroSettlementForRefunded(marketType)` — 종결 lineItem 정산0+verified, 멱등.
- 배선: 스토어(빈 postSyncProcess였음)·11번가·Cafe24(GMARKET+AUCTION 둘 다)·쿠팡(CANCELED 커버) postSyncProcess.
- 테스트: `TerminalSettlementServiceTest` 5건. 생성자 파라미터 추가로 깨진 기존 테스트 7파일 목 주입 보정.

## 판정·게이트
- 회귀: core+infra+api 전체 BUILD SUCCESSFUL.
- 표준 등급(다마켓·정산) → 회귀 통과 확인. 사용자가 라이브 읽기 승인 하에 진행.
- **라이브 검증**: 배포(23:55:02Z) 후 스토어·Cafe24 동기화 → 로그 "[GMARKET] 1건·[SMART_STORE] 2건". DB: 곽금희 26611→0, 정가영 78140→0, 이명동 47590→0 전부 verified. 오작동 0.

## 미해결·보류 (Phase B — 상태 감지 보강, 라이브 API 의존)
정산0(Phase A)은 완료. 상태 **감지** 보강은 마켓별 라이브 API 작업이 남음:
- **스마트스토어**: 어댑터가 `claimStatus`/`claimType`를 안 읽어 반품/교환(현재 취소만)을 놓침. `POST product-orders/query` 응답 실구조(최상위 vs `content` 하위) 라이브 확인 → 어댑터·매퍼 보강 필요. (별도 API 불필요, 파싱·매핑 층)
- **11번가**: 클레임 조회 REST 실재 여부가 선결. 셀러 OpenAPI에 취소/반품 목록 조회 엔드포인트 라이브 확인 → 있으면 쿠팡 detectReturns 패턴 이식, 없으면 absence 감지가 전부 CANCELED로 뭉뚱그리는 한계 유지(반품/교환 구분 불가, 배송완료후 반품 미감지).
- **Cafe24**: 이미 작동. C/E 코드값 화이트리스트 정밀화(현 prefix매칭)·취소주문이 조회에서 사라질 때 detectCancellations 필요 여부 정도.
- **교환 정산**: EXCHANGED는 정산0 제외했으나, 교환 시 추가 배송비·차액 처리는 별도 판단 후보.

## 다음 단계 참조
- Phase B 착수 시 **스마트스토어부터**(경로 명확, claimStatus 파싱 보강) 권고. 11번가는 API 실재 조사가 선행.
- 11번가 클레임 API 라이브 조사는 셀러 openapikey로 `ordservices` 계열 취소/반품 엔드포인트 탐색부터.
- 원장 `발견` 상태 잔여 결함 확인 후 다음 배치 판단.
