# 정상화 사이클 결과서 — 2026-08-22 17:30 (P3 사이클 A: D-176·177·178·180a)

## 처리 결함

| 결함 | 등급 | 결과 | 커밋 |
|------|------|------|------|
| D-176 날짜 필터 UTC 계산(KST 00~09시 전날 밀림) | P3 / 표준 | 검증통과·배포·라이브 완료 | 8b2810d |
| D-177 마켓 라벨 맵 불일치(사용자 판정: 각각 표기) | P3 / 표준 | 검증통과·배포·라이브 완료 | 8b2810d + 7b25758 |
| D-178 소싱 초안 목록 화면 부재 | P2 / 표준 | 검증통과·배포·라이브 완료 | d825b57 |
| D-180a 배치 트리거 응답 키셋 비대칭 | P3 / 표준 | 검증통과·배포(라이브는 계약 테스트 갈음) | 400b6b1 |

## 수행 구조

fixer 3명 병렬(영역 상호 불가침: grid=D-176·177 / drafts=D-178 / batchkeys=D-180a BE) → verifier-p3a 통합 검증 전건 PASS → 리더 게이트(백엔드 1,244 테스트·FE tsc·build) → 결함 단위 3커밋 push → 소사이클 1건(DraftReviewPage 라벨, 경량 리더 검수) 추가 커밋 → 배포 확인 → Chrome 라이브 검증.

## 핵심 결정과 이유

1. **주문 마켓 라벨 vs 동기화 채널 라벨 개념 분리** (fixer 제안, 리더 승인): 사용자 판정 "각각 표기"는 주문의 표기에 대한 것. 동기화는 백엔드가 GMARKET 단일 키로 G마켓·옥션 양쪽을 기록하므로 채널 표기(`SYNC_SOURCE_*`)만 'G마켓/옥션' 통합 유지 — 점 행·SSE 토스트 문구는 수정 전과 동일(행위 무변경). `renderMarketType`(액션로그 마켓 컬럼)은 정본 유지 확정 — 옥션 주문 행에 통합 라벨이 들어가면 판정에 정면 위배.
2. **D-176 5번째 지점**: 지시된 4지점 외 기간 프리셋(`handlePeriod`)도 동일 UTC 결함 — 타입게이트가 검출, 동일 수정 승인.
3. **D-178 직렬화 함정 재확인**: 전역 apiClient에 paramsSerializer 부재가 실측으로 확정 — `drafts(status[])`도 개별 serializer로 회피(D-175 교훈의 재적용). **전역화가 근본 해소 후보**.
4. **소사이클**: 검증 중 발견된 DraftReviewPage의 5번째 라벨 맵(정본과 값 불일치·3키 부재로 원문 코드 노출)을 같은 사이클에서 fixer-p3-drafts가 치환(7b25758) — D-177 잔여 소진.

## 검증·라이브 요약

- verifier-p3a: D-180a Red 독립 재현(git archive, 4 FAILED 대조)·가산성·count 동일 원천 실증 / D-176 `toISOString` 날짜 용도 잔존 0 + KST 경계 19케이스 실행 그린 / D-177 3벌+인라인 소거·동기화 문구 무변경 대조 / D-178 axios 직렬화 실행 채증(serializer 없으면 `status[]=` 확인).
- 라이브(배포 7b25758, 컨테이너 17:20 재생성): 주문그리드 기본 범위 07.22~08.22, '옥션' 개별 배지, 진행 현황 액션 'G마켓/옥션 동기화'+마켓 'G마켓', /sourcing/drafts 렌더 정상(운영 초안 0건·API 200 `[]` 교차 확인).

## 신규 등재 (이번 사이클 발견, 미수정)

- **D-199** (P4): `kstDateStringOffset` 월말 오버플로 — 3/31만 3일, 나머지 월말 4일 각 1일, 연 5일 발생. 체감 미미.
- **D-200** (P3): 동기화 점 행 CUSTOMS 채널 미렌더 — 백엔드 7키 vs 프론트 6키, 선존 공백.

## 미해결·보류

없음. D-180 묶음의 나머지(헬퍼 중복·set-state-in-effect·rename·OrderGrid 분해)와 D-179는 사이클 B(구조 변경)로.

## 다음 단계 참조

1. **사이클 B(구조 변경) 착수**: D-179 죽은 코드 묶음 + D-180 구조 잔부채(requireNonNegative 중복·set-state-in-effect 5건·productMockApi rename). OrderGrid 2,065줄 분해는 별도 독립 사이클 유지. D-179의 AsyncConfig는 `@EnableAsync` 담당 여부 검증 후 판단, 프론트 §C 미호출 API 7건은 BE 엔드포인트와 동반 판정.
2. paramsSerializer 전역화(axios.ts) 검토 — 개별 적용 2곳(productApi·sourcingDiscoveryApi)을 전역 1곳으로. 사이클 B 구조 항목에 편입 가능.
3. D-199·D-200은 P3 이후 백로그. 카테고리 미분류 2,858행 건은 여전히 사용자 판단 대기.
