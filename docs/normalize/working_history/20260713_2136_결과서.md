# 2026-07-13 21:36 결과서 — 쿠팡 E2E 실전 테스트(구매정보→이메일 모니터링→송장 대조/배송) + 배포 정상화

## 개요
쿠팡 주문 **14101552820428**(order_id=178, lineItem=409)에 대해 실제 운영 DB·API·Gmail·쿠팡 API를 사용한 라이브 E2E를 수행했다. 구매정보 업데이트 → 이메일 모니터링 등록 → iHerb 발송메일 수신·송장 파싱 → 시스템 송장과 대조 → 배송처리 전 과정을 실행하며 결함 6건을 발굴·수정했다. 병행으로 반복되던 배포 컨테이너 충돌을 근본 해결하고 git push 자동배포(CI/CD)를 구현했다.

## 처리 결함
| ID | 구분 | 내용 | 수정 |
|----|------|------|------|
| D-E1 | 설정 | iHerb Gmail IMAP 자격증명 미설정(EMAIL_USERNAME_1/PASSWORD_1 빈값) → 이메일 모니터링 동작 불가 | 서버 `~/projects/.env`에 gootkimjw8712 계정 앱비밀번호 등록 |
| D-E2 | 설정 | 배포 compose가 구버전 `EMAIL_ADDRESS/PASSWORD`(단수) 매핑 — 코드는 `EMAIL_USERNAME_1/2`+`_2` 기대 | `~/projects/docker-compose.yml` sbshop-api env에 `EMAIL_USERNAME_1/2`·`EMAIL_PASSWORD_1/2` 매핑 추가 |
| D-E3 | 행위 | worker에 `spring-boot-starter-web` 누락(레거시 병합) — `server.port:8081`·Dockerfile EXPOSE·start.sh 전제였으나 HTTP 엔드포인트 불가 | web 스타터 복구 + `EmailFetchController`(POST `/internal/email/fetch`, 컨테이너 로컬 8081) 신규 |
| D-E4 | 행위 | 빈 `EMAIL_USERNAME_2` 계정으로 Gmail 로그인 실패 반복 | `searchInAccountForOrderNo`에서 빈 username 계정 스킵 |
| D-E5 | 행위 | 쿠팡 롯데택배 코드 `LOTTE` 전송 → 400 "Delivery company code not supported" | `CoupangOrderAdapter.mapCarrierCode`: `LOTTE_LOGISTICS → "HYUNDAI"`(쿠팡은 롯데택배를 구 현대택배 코드로 식별). 회귀테스트 추가 |
| D-E6 | 행위 | 주문이 배송중/배송완료로 넘어가면 쿠팡이 송장 처리 거부("배송진행상태가 유효하지 않습니다") → 재시도 가능 실패로 처리돼 30분마다 영구 재시도 | `MarketShippingResult.terminal` 추가, 잠금 메시지를 terminal 분류, `EmailFetcherService.handleMarketResult`로 3개 분기 통합 — terminal 시 전송종결 마킹(재시도 중단)+ActionLog 감사기록. 회귀테스트 추가 |

## 판정 요약
- 코드 컴파일(core/worker/api) + `:core:test` 전체 통과. 신규 회귀테스트 2건 통과(CoupangOrderAdapterCarrierCodeTest, MarketplaceShippingTerminalTest).
- **라이브 검증**: 구매정보 PATCH→모니터링 쿼리 자동 포함 확인 / IMAP 연결·이메일 파싱(tracking=315398790560, 롯데) 성공 / 송장 대조 분기 정확 / D-E5·D-E6 배포 후 재트리거로 실동작 확인(2차 트리거 시 "처리 필요 주문 없음" = 재시도 루프 차단). order 409 최종: 실송장 DB 기록 + terminal 마킹(tracking_sent_to_market=t).

## 커밋
- `1ad7853` D-E3/E4(worker web 트리거+빈계정 스킵)
- `e4ccf43` D-E5(롯데 HYUNDAI 코드)
- `23316c9` D-E6(배송상태 잠금 종결)
- `25c0f6e` CI(자동배포+컨테이너 충돌 차단)
- 서버 인프라(비-repo): `~/projects/.env`·`docker-compose.yml`(D-E1/E2), `~/webhook/deploy.py`·`deploy-sbshop.sh`(CI/CD). 모두 `.bak-e2e` 백업 존재.

## 배포 정상화(사용자 요청)
- **컨테이너 이름충돌 근본원인**: compose `container_name` 고정 + `up --build` recreate가 `<hash>_projects-sbshop-api-1` 잔여를 남겨 반복 충돌(Docker29/Compose v5). → **"제거 후 생성"**(build→rm -f→up) 안전 스크립트로 원천 차단.
- **CI/CD**: sbshop-agent repo GitHub 웹훅(:9000, push) 추가 + `deploy.py` 저장소별 라우팅·즉시200·백그라운드 배포·**docs-only 스킵**. main push 자동배포를 라이브 검증(25c0f6e 자동 반영, 충돌 0).

## 다음 단계 참조
- **미구현/후속**:
  - webhook 배치-실행중 가드 없음 — push=즉시배포라 라이브 배치 중 push 시 중단됨([[deploy-interrupts-running-batch]]). 필요 시 배포 전 `/api/v1/products/batch/status` 확인해 defer하는 가드 추가 검토.
  - D-E6 종결 마킹은 `trackingSentToMarket=true` 재사용(스키마 변경 회피). 배송중 vs 배송완료 구분이 필요하면 전용 상태/필드(수동 DDL) 검토. 현재는 ActionLog로 실제성공과 구분.
  - iHerb 이메일 파싱: 국제 택배사(DHL/FedEx/UPS/EMS)는 ETC 매핑되어 쿠팡 코드 손실 가능 — 실제 발생 시 매핑 보강.
  - 송장번호 정규식 `trackingNumber=(\d+)`는 숫자만 — 영문/하이픈 송장 미지원(현재 iHerb는 숫자라 무관).
- **라이브 잔여**: 다른 iHerb 주문(배송지시 상태)에서 정상 송장 전송(HYUNDAI) 성공 케이스 관찰로 D-E5 성공경로 확인 권장.
