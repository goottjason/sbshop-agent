# 세션 인계 — 2026-08-28

계정 전환·세션 클리어 전 인계 문서. **다음 세션은 이 문서부터 읽을 것.**

## ⚠ 사용자가 "놓치지 말라"고 명시 지정한 2건

### ① 포트넘(FTN) 크롤 고속 경로 전환 — 40배 + 부가정보
- 현행 `scraper/scrapers/fortnum.py`는 브라우저 렌더링으로 **상품당 20초**.
- **Magento 2 GraphQL** `https://www.fortnumandmason.com/graphql` (**GET 공개 / POST 차단**) 사용 시 **0.5초**.
- 한 호출로 **원가(`cost`)·중량(`weight`)·HS 관세코드(`commodity_code`)·성분·알레르겐·원산지** 동시 확보. 현재 이 값들이 비어 있다.
- 영향: FTN 393건의 재고·가격 크롤 전반. 근거는 원장 D-219 "부수 발견" 절.

### ② 수집한 바코드를 쿠팡에 반영
- 바코드 2,203건이 **DB에만 있고 마켓에 안 나갔다.** `CoupangProductPayload.Item`에 barcode 필드가 **아예 없다**.
- 쿠팡 규약 3필드 세트: `barcode` / `emptyBarcode`(없으면 true) / `emptyBarcodeReason`(≤100자). **"그냥 비움"이 아니라 emptyBarcode=true+사유가 정규 경로.**
- 바코드는 **승인필요 PUT에서만** 변경 가능 → 상품이 심사중으로 전환(D-216). 실측 150건에서 3값 일치·롤백 위험 0이라 가격 훼손 우려는 낮다.
- **선행 조건**: 쿠팡 seller gating 해소. 지금 밀어넣으면 전부 심사 대기에서 멈춘다.
- 근거: 원장 D-217 ④절.

## 즉시 실행 가능한 것 (조사 완료, 착수만 남음)

- **COK 바코드 26건 수집** — `GET https://www.costco.co.uk/rest/v2/uk/products/{code}?fields=FULL` → **`.ean`**. code=상품 URL `/p/` 뒤 마지막 세그먼트, 404 시 `{code}_BD` 폴백. **1초 이상 페이싱 필수**. 전량 2~3분.
  - **⚠ `upc` 키는 값이 전부 `'0'`인 쓰레기값. 진짜는 `ean`이고 값 없으면 키 자체가 사라진다.**
  - **⚠ 가짜 필터 필수**: `1000000`+사내품번 6건은 플레이스홀더. 체크디지트 실패 5건·12자리 보류 1건(`512454063291`)도 배제.
- **920건 `emptyBarcode=true`+사유 처리** (FTN 393·OCD 407·TES 98·COK 22) — 수집 경로가 없음이 확정된 건들.
- **사고 주문 `4477134670` 기록 교정** — 아직 `product_id=2320`(200828TE001)을 가리킨다. 정답은 `2478`(230806IHB130). 조회 창 밖이라 자동 교정 안 됨. **사용자 승인 후 수동 교정.**
- **승인 도구 응답 분류 수정** — 쿠팡 `code=ERROR / "IN_REVIEW by seller gating"`을 지금 "실패"로 집계하는데 **실제로는 접수되어 상태가 바뀐다.** "접수됨(심사 대기)"으로 재분류 필요. 안 고치면 대량 실행 시 성공을 전부 실패로 센다.

## 사용자만 할 수 있는 것 (가장 중요)

**쿠팡이 판매자 대표 메일로 보낸 판매중지 사유 확인·회신.** D-209에서 판매재개 시도가 945회 거부됐고("쿠팡에 의해 판매중지된 상품"), 승인 파일럿도 `seller gating`으로 심사 대기에 묶였다. **이게 안 풀리면 승인 요청도 바코드 반영도 전부 막힌다.** 스팸함 확인, WING>온라인 문의로 재발송 요청 가능.

## 현황 요약

**바코드**: 3,195건 중 **2,203건(69%)** 확보(착수 전 0건). IHB 2,132/2,146(99.3%) · VTB 71/102(70%) · 나머지는 위 참조.

**쿠팡**: 임시저장 1,029건은 "안 팔림"이 아니라 **과거 승인분이 팔리는 위에 미제출 초안이 얹힌 상태**(D-215). 승인 요청 도구로 **임시저장→심사중 전환은 실증**됐으나 하루 지나도 승인완료 미도달. 실판매 표본 150건 중 **113건(75%)이 NOT_ON_SALE**.

**식별자 정합성**(D-210 리포트): 카페24 3,184/3,184 완전일치 · 스토어 불일치 43·중복 43·마켓에만 10 · 쿠팡 우리에만 101 · **11번가는 D-208로 비활성(2,286건 미판정)**.

**최근 P0**: D-218 오배송(부분 문자열 매칭) 수정·배포·검증 완료.

## 미해결 백로그 (원장 참조)
D-176~180 잔여(OrderGrid 분해 완료, 스타일·toast 일부) · D-199 월말 오버플로 · D-200 CUSTOMS 점 · D-202 alert 6건 · D-207 스토어 판매중지 · D-208 11번가 응답검사 미해소(쓰기 3경로 무검사) · D-209 쿠팡 판매중지 · D-211 11번가 sellerPrdCd 미전송 · D-212 11번가 삭제 오기록 · D-213/214 · D-216 승인PUT 파일럿 미실행 · D-219

## 운영·작업 규율
- 가격 정책 = **마진 5% / 쿠폰 0% / 최소마진 1,500원**(2026-08-24 전량 적용).
- **배치 중 `git push` 금지** — 자동배포가 JVM을 재시작해 배치를 끊는다. 바코드 배치는 멱등이라 재개 가능.
- 배포 확인 = 서버 커밋 **8자리** 해시 일치 + 컨테이너 생성시각 epoch > 커밋시각.
- 공유 트리 gradle 경합(`NoClassDefFoundError`·`Could not write XML test results`)은 결함 아님 — `--init-script`로 buildDirectory 격리 또는 1회 재실행.
- 운영: `ssh -i ssh-key-2026-06-25.key ubuntu@168.107.31.154` · `docker exec projects-postgres-1 psql -U canagent -d sbshop` · 앱 `http://168.107.31.154/sbshop-agent/`
- **SSH 키 유실 주의**: 2026-08-24 중복파일 정리 도구가 휴지통으로 옮긴 사고. 사라지면 `~/Downloads/ssh-key-2026-06-25.key`에서 복구 후 `chmod 600`.
