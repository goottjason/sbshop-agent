# 2026-07-17 16:13 결과서 — 이메일 펫칭 계정 정상화 (다계정 확장 + 설정 통일)

## 개요
`order_list.xlsx` 구매정보 채우기에서 추출한 **소싱 이메일 23개** 중 시크릿(앱 비밀번호)이 3개뿐이라 나머지 계정 펫칭이 불가한 문제를 정상화했다. 조사 결과를 `docs/normalize/email-accounts.md`로 정리하고, 계정 수 고정(yml 3슬롯) 제약을 `EMAIL_ACCOUNTS` 단일 env로 해소했다. 관련: [[iherb-email-tracking-pipeline]].

## 아키텍처 실측 정정
- 문서 전제 "펫칭이 Gmail 하드코딩 전용"은 부정확. **코드(`EmailFetcherService`)는 이미 계정별 host/port/protocol 지원**. 진짜 제약은 `application.yml`이 **슬롯 3개(worker)/2개(api) 고정 + host `imap.gmail.com` 고정**한 것.

## 사용자 결정
| # | 사안 | 결정 |
|---|------|------|
| ① | 유사 계정군(younzara@ 4제공자, kim* 군) 성격 | **모두 별개 사서함**(23개 각각 실계정) |
| ② | 비-Gmail 9개 IMAP 지원 | **코드는 Gmail만**, 비-Gmail은 대표 Gmail로 **포워딩** 우회 |
| ③ | 계정 확장·.env 정리 | `EMAIL_ACCOUNTS` 단일 env 도입 + `.env` 4종 통일 |

## 처리 내용
| 구분 | 내용 |
|------|------|
| 코드(TDD) | `EmailAccountProperties.parseCompactAccounts()` 신규 — `EMAIL_ACCOUNTS`(`user:pass` 목록, 쉼표·줄바꿈 구분)를 Gmail 계정으로 파싱. 공백 앱비번 보존, 빈/오류 항목 스킵. `@Value`+`@PostConstruct`로 yml 슬롯과 병합(하위호환). |
| 테스트 | `EmailAccountPropertiesTest` 6케이스(blank/단일/혼합구분/공백비번/스킵/트림). Red→Green 확인. |
| 설정 | `api/application.yml` 슬롯 3개로 통일(worker 일치) + 두 yml에 `EMAIL_ACCOUNTS` 안내 주석. |
| 설정(비커밋) | 로컬 `.env` 4종 통일: 루트 레거시 `EMAIL_ADDRESS`/`EMAIL_PASSWORD`(죽은키, 참조0) 제거, backend/api/worker를 루트 기준 3슬롯으로 정렬. `.bak-emailnorm` 백업(gitignore). |
| 문서 | `docs/normalize/email-accounts.md` — 23계정 인벤토리·시크릿유무·결정사항·잔여 수동작업. `.env.example` 형식 안내. |

## 판정 요약
- `:core:test`(파서 6케이스) + `:worker:test`(EmailFetcherServiceTest) + `:api:test`(ApiContextLoadSmokeTest 등) 통과 → **BUILD SUCCESSFUL**.
- 컨텍스트 로드 스모크 통과 = `@PostConstruct`/`@Value` 배선이 실제 Spring 컨텍스트에서 정상.
- 리스크 등급 **표준**(다파일 설정+코드, 마켓 계약·스키마 무변경). `_workspace/.autonomous` 자동 통과. push 대기.

## 다음 단계 참조
- **사용자 수동 작업(코드 밖)**:
  - Gmail 11개 앱 비밀번호 생성(2단계 인증 필요) → 서버 `~/projects/.env`의 `EMAIL_ACCOUNTS`에 `user:pass` 추가.
  - 비-Gmail 9개(hanmail4·naver3·nate1·skku1·apple1) → 대표 Gmail로 IMAP/자동 포워딩 설정. 포워딩 후엔 별도 계정 등록 불필요(대표 Gmail 인박스로 유입).
- **검증 권고**: 계정 추가 후 `docker exec ... curl -XPOST localhost:8081/internal/email/fetch`로 펫칭 실동작 확인([[iherb-email-tracking-pipeline]]).
- **주의**: 같은 계정을 yml 슬롯과 `EMAIL_ACCOUNTS`에 중복 등록하면 이중 펫칭. 서버는 `EMAIL_ACCOUNTS` 단일 소스로 이관 권장(슬롯 비우기).
- **미착수**: 재진입 가드(F-MISC-18)는 인-JVM만 — cross-JVM(api/worker 동시) 이중처리 여지는 별건으로 남음.
