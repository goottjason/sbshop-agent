# 정상화 결과서 — 2026-07-16 16:11 · R1 오류·HTTP 시맨틱 (인프라 + 병렬 2웨이브)

R5·재무·Cafe24스킵·R2에 이어 **R1(오류·HTTP 시맨틱)**을 처리했다. 계약 영향이 커 프론트 사용처를 사전 검증하고 **비파괴 접근**(프론트가 body/200을 소비 → status 변경 최소화)으로 진행. push 대기.

## 접근
- 프론트 axios에 **인터셉터 없음**(각 호출 로컬 catch), `BatchUpdatePage`는 이미 404를 폴링중단으로 처리 → body shape 유지·status만 조정 시 리스크 낮음.
- Phase A(인프라, 리더) → Wave 1(404 변환, 병렬 3) → Wave 2(예외·부분실패·애드혹, 병렬 6).

## 처리 결과

### Phase A — 인프라 (`9593a2e`)
`ResourceNotFoundException`(core) + `GlobalExceptionHandler` 404 매핑. body({success:false,message}) 유지. 순수 additive.

### Wave 1 — 미존재 리소스 404 (`a517516`)
| 결함 | 처리 |
|------|------|
| F-PROD-5·26/29 | 상품 조회/수정/삭제 미존재 id 5사이트 → 404(입력검증 400은 유지) |
| F-BATCH-S2·SM1 | getBatchStatus/getBatchSummary 미존재 batchId → 404, **진행중(total>0)은 200 유지**(폴링 계약 보존) |
| F-MREG-2 | MarketRegistrationService 상품 존재검증 → 미존재 404, 등록0건은 200 유지 |

### Wave 2 — 예외 보존·부분실패·애드혹 (병렬 6)
| 결함 | 처리 | 커밋 |
|------|------|------|
| F-ORD-8 | 마켓 접수 실패 → MarketOrderAcceptException(유형·cause 보존, 500 불변) | `cae6059` |
| F-CAFE-2 | /status 인증실패는 200 유지, 진짜 인프라오류만 5xx 전파(비파괴) | `c5a7ee4` |
| F-MISC-20 | 이메일 fetch 재진입 스킵/실행 구별 executed 필드(void→boolean, 내부 엔드포인트) | `bea75d1` |
| F-MISC-11 | ProductSync ResponseEntity<?> → Map<String,Object> 명시화(바디 불변) | `825ae48` |
| F-BATCH-B2 | /by-supplier 0건·정상 응답 스키마 {batchId,count,message} 통일(tsc 통과) | `2df76de` |

### 오탐/유지
- **F-ORD-33**: bulkShipOrders null 가드 기존재(빈 결과) — 회귀 테스트만.
- **F-ORD-12**: 부분실패 이미 200 body(BulkConfirmResult)로 표면화·프론트 3분기 처리. status 변경은 파괴적 → 유지.

## 판정 요약
- 계약 안전: 모든 변경이 프론트 사용처 검증 후 비파괴(정상/만료 200 유지, 미존재만 404, 진짜 서버오류만 5xx). 프론트 코드 변경 0, tsc 통과.
- 게이트: 각 웨이브 리더 단독 `./gradlew test` 전건 SUCCESSFUL(각 ~2m20s). 병렬 fixer 격리로 mid-edit 경합 회피.
- 커밋: 인프라 1 + Wave1 1 + Wave2 5 + docs. 신규 테스트 다수(404·계약특성·예외유형).

## 다음 단계 참조
- **다음 배치 권고: R6(조회 품질, 낮)** — 목록 정렬·페이징·필터 일관, 조회계 입력검증 잔여(F-SUP-4·LC-2·BATCH-S3·ST2·MISC-1·2, F-ORD-3, F-PROD-1·18·22, F-PSRC-5, F-S4). 리스크 낮고 병렬화 쉬움.
- 그 다음 R3(정합·멱등성, 높·건별 재현) → R4(sync 엣지, 높·라이브 확인) → R7(보안 선택) → R8(문서).
- quick-win 후보: 변경감지 스킵 쿠팡 확장.
- **배포 유의**: R1은 상태코드 일부 변경(미존재 400/200→404, Cafe24 인프라오류→5xx)이나 프론트 비파괴 검증 완료. 라이브 확인 권장 항목: 상품/배치 미존재 조회 시 404 표시, Cafe24 status 정상/만료 200 유지. 누적 대기 커밋 다수 — 배포 시 일괄.
