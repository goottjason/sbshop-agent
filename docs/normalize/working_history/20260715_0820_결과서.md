# 정상화 결과서 — 2026-07-15 08:20 · SP-11 잔여 + SP-8 + 저순위 검증 배치 (세션 마무리)

## 이번 사이클 처리

### SP-11 잔여 중복제거 (구조, 동작 불변)
| 결함 | 처리 | 커밋 |
|------|------|------|
| F-BATCH-3·B3 | 트리거 4종 startBatchWithLog 헬퍼 통합 | `682d95f` |
| F-PROD-15·19·20 | 이미지 3경로 공통 헬퍼(부분실패 보존) | `5549f67` |
| F-CAFE-12 | 토큰교환 exchangeAuthorizationCode 헬퍼 | `87bb414` |
| F-SYNC-5 | DTO 트리오 dispatch 골격만 통합(Cafe24·마켓별 로직 보류) | `baad6ff` |

### SP-8 삭제 시맨틱 (사용자: 둘 다 지우기)
| F-ORD-23 | 빈문자열 클리어 이미 동작(오탐) — 회귀고정 | `a6d2759` |
| F-PROD-13 | 빈 리스트=이미지 전체삭제 | `70cfe2c` |
→ SP-8 완결(F-S2·F-ORD-23·F-PROD-13).

### 저순위 입력검증 배치 (15건, 행위 변경)
| 도메인 | 항목 | 커밋 |
|--------|------|------|
| order | F-H4(송장필수)·ORD-22(빈주문 확인차단)·ORD-26(유니패스 필수)·ORD-2(기간필터 경계) | `aff9814` |
| product | F-PROD-8·23(음수검증)·11(빈이미지)·7(soldOut null 오전파 차단) | `c41dee3` |
| batch/sourcing/supplier | F-BATCH-4·PSRC-1/7/11·SUP-CS-1/2·2(null·bulk·중복·상태필터 검증) | `3970dd1` |

- **TDD·단독 통합 회귀(core·api·infra)·프론트 tsc 전건 통과**. 병렬 fixer는 격리 worktree·단독 게이트로 상호간섭 회피. 원장 무단수정 되돌림(추적 api-analysis 단일원본).
- 병렬 실행 시 "Could not write XML"·testcontainers 타임아웃은 Gradle 동시실행 경합 아티팩트 — 단독/max-workers=1 재실행 시 SUCCESSFUL(테스트 실패 아님).

## 시스템성 패턴 최종 (SP-1~11)
- ✅ **완료 9**: SP-1·2·3·4·5·6·8·9·11
- 🔶 **부분 2**: SP-7(bad-enum·조용204 해소 / 404 시맨틱 잔여), SP-10 축소(마스킹·internal가드 / CORS·PII·OAuth는 보안 비중요로 범위 밖)

## 결함 집계 (FINDINGS-CHECKLIST)
- **해결 78 · 부분/오탐 6 · 미해결 123**
- 미해결 123은 대부분: (a) 보안류(F-CAFE OAuth state·F-SYNC-13 preview PII·F-CRED-2/9·F-MISC-12/13 — 사용자 보안 비중요), (b) 순수 NOTE(설계 관찰·의도 가능), (c) 설계결정 필요(F-ORD-8 예외유형·F-PSRC-13 게시멱등성·F-SYNC 분산tx 등), (d) 저가치 SMELL. **명확한 버그·입력검증 quick-win은 소진.**

## 미해결 후속 후보 (문서 백로그)
- 보안 강화 원할 시 SP-10 전체(OAuth state·CORS·PII·시크릿 암호화).
- 분산/멱등성: F-PSRC-13(게시), email 완전멱등성, F-SYNC-20(customs 부분롤백 잔여).
- sync 엣지: F-SYNC-6(취소감지)·10·11·12, F-MREG-1/2/5/7.
- 정리: getAllBatches 죽은코드, ProductSync 상품별 크롤예외, manual-update-all 쌍객체.
- 각 항목은 FINDINGS-CHECKLIST 체크박스로 남아 언제든 이어서 가능.

## 배포 유의 (일괄 push 시 — 변동 없음)
ddl-auto=update 첫 기동(sb_market_sync_status 자동생성)·배치중단·라이브검증(SSE·sync상태·400 메시지·Cafe24 재인증). 이번 배치의 일부 200→400 전환(송장필수·유니패스 필수·음수거부·null검증) 프론트 표시 확인 권장.
