# 2026-09-20 02:50 결과서 — 배포 2단계(GHCR pull) 완료, CI 테스트 게이트

## 처리
| 항목 | 내용 | 커밋 | 상태 |
|------|------|------|------|
| D-317 A | `images.yml`: arm64 러너에서 3개 이미지를 빌드해 GHCR 에 푸시(각 2분 안팎, 패키지 공개, 서버 익명 pull) | 0aedafff | 라이브 |
| D-317 B | `deploy.sh` pull 방식(`IMAGE_TAG`), 두 단계 pull, 지문에서 라벨 제외, 접두어·태그 검증, 12자리 커밋 태그 롤백 | 51962fe3, d9c22497 | 검증 PASS, 라이브 |
| D-317 C | `deploy.yml`: validate → tests ∥ images → deploy(pull) | 4e3651ae | 라이브 5경로 |
| D-318 | 429 쿨다운 테스트가 Linux 에서 실패(Instant 나노초 정밀도) — 단언 쪽 기대값을 밀리초로 자름 | fd3c511f, 06e01e85 | CI 초록 |
| D-319 | Cafe24 토큰 테스트가 UTC 에서 실패(운영은 KST 명시 해석) | 40173965 | CI 초록 |

## 판정 요약
- 검증자 재검토 4: 조건부 PASS. 지적 반영: 지문의 라벨 제외(실서버에서 라벨만 다른 이미지로 확인), `REGISTRY_PREFIX` 검증, pull 실패 시 정리, 재태그 실패 분기 테스트, `^` 앵커 테스트, 워크플로 입력 검증·롤백 인자 전달.
- 게이트: 셸 테스트 259, 변형 18건 사망. CI 테스트 백엔드·프런트 초록(전체 모듈 `--continue`). 워크플로 자체는 검증자 정적 검토 없이 실서버 5경로 실행으로 검증.
- 라이브 검증: ① push 전체(테스트 6분20초 ∥ 빌드 → 서버 pull·교체 1분24초, 총 약 8분) ② 레지스트리 커밋 태그 롤백(~20초) ③ `image_tag` 재배포(api만 교체) ④ 잘못된 입력 2종 거절 ⑤ `skip_tests` 경로.

## 핵심 결정과 이유
- pull 한 이미지를 기존 로컬 이름으로 다시 태그 — compose·지문·prev 태그·nginx·헬스 로직 무변경, 변경 범위 최소.
- 지문에서 라벨 제외 — 커밋 SHA 라벨이 매 배포 전체 교체를 유발.
- 패키지 공개 유지 — 비공개면 4GB scraper 가 무료 한도(500MB)를 넘고 서버에 토큰이 필요.
- 테스트를 배포 게이트로 — CI(UTC·Linux)가 로컬이 숨긴 테스트 결함 2종을 드러냈고 수정해 초록 확인 후 게이트로 연결. 긴급용 `skip_tests`.

## 미해결·보류
- 사용자: Oracle 콘솔 9000/tcp 인그레스 삭제. 프런트 `package-lock.json` 커밋 여부(현재 `.gitignore`, 의존성 미고정).
- 3단계(무중단 교체·자동 롤백) 미착수. can-agent·life-change 워크플로 표준화. 옛 릴리스 폴더 ~1.1GB 정리(승인 필요). 서버 빌드 캐시 ~10GB(정비 스크립트 대상).
- `spotlessCheck` 기존 52개 파일 위반(게이트 아님).

## 다음 단계 참조
1. 사용자 결정: 프런트 lockfile, 3단계 착수 여부.
2. can-agent·life-change 배포를 같은 구조(Actions 테스트→빌드→pull)로 표준화 검토.
3. 옛 릴리스 폴더·빌드 캐시 정리(승인 후).
