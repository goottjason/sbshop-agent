# 사이클 2 결과서 — 2026-07-07 (구조 변경 배치)

## 처리 결함 (2건 검증통과 → 커밋)

| 결함 | 등급 | 처리 | 커밋 |
|------|------|------|------|
| D-004 | P2/표준 | 이미지 다운로더 3벌 → ImageDownloadService 1벌(ImageDownloadClient 구현), ProductController 인터페이스 주입, ImageDownloader 삭제 | caca5c6 |
| D-010 | P3/표준 | ElevenstOrderRestClient/ElevenstMarketRestClient 역할 기반 리네임, 빈 이름 정합 | 3196b1d |

## 판정 요약

- 내부 QA 2/2 PASS (D-010은 부분→승격). 리더 게이트 `./gradlew test --rerun-tasks` 전체 그린.
- 동작 불변 증명: 특성화 테스트(ImageDownloadServiceCharacterizationTest — JDK HttpServer, 신규 의존성 0)로 출력 계약·Cloudflare 우회 헤더(User-Agent/Accept) 고정. 승인된 유일 행위 델타: ProductCreateUseCase 다운로드 경로 RestTemplate→OkHttp+UA (리더 지시 방향).
- 자율 모드 커밋 (표준 등급 — `_workspace/.autonomous`).

## 핵심 결정과 이유

1. **spotlessCheck 게이트 제외**: 전역 기존 위반(파일 다수)으로 신규 코드와 무관하게 실패. D-014(전역 spotlessApply 단일 커밋 배치)로 분리 — 구조 diff 오염 방지.
2. **D-010 양쪽 리네임**: 한쪽만이 아닌 양쪽을 역할명으로 리네임해 클래스명↔빈이름 일치. 기본 빈 이름이 한정자와 일치하게 되어 한정자 제거(빈 이름 무변경).

## 운영 사건 2건 (하네스 개선 입력)

1. **중복 fixer 스폰**: 리더가 무응답 팀원을 오종료 판정해 동일 배치에 두 번째 fixer를 스폰 → 동시 편집 위험. 두 번째 인스턴스가 충돌을 감지하고 자발 중단·독립 검증만 남기고 종료. 교훈: 무응답 판정 전 원장/파일 mtime 재확인, 동일 임무 재스폰 금지.
2. **검증자 트리 오염**: qa-verifier가 검증 중 spotlessApply(모듈 전역)를 실행해 무관 42파일 재포맷 → 자가 원복 + fixer 정리로 해소. 교훈: 검증자는 트리 변경 명령 금지(읽기+테스트 실행만) — 에이전트 정의에 명문화 필요.

## 미해결·보류

- 보류: D-002(오판). 미해결 6건: D-005(중대), D-007, D-008, D-011, D-013, D-014(신규 — spotless 전역 위반)

## 다음 단계 참조

- **사이클 3 배치(행위 변경)**: D-011(BatchPriceStockService에 `@Async("productBatchExecutor")` 한정자 — D-003 후속 배선) + D-013(api 테스트 datasource 구성 — H2 격리 config 우선, testcontainers는 신규 의존성이라 사용자 승인 필요).
- **사이클 4 후보**: D-014(전역 spotlessApply 단일 커밋 — 순수 포맷, 다른 변경과 절대 혼합 금지) / D-007·D-008(프론트 P3).
- D-005(중대·스키마)는 자율 모드에서도 사용자 승인 필수 + 운영 DB 실상태 확인 선행.
- **하네스 개선 반영 예정**: ①오케스트레이터 에러 핸들링에 "무응답 재스폰 전 mtime 재확인" ②qa-verifier 정의에 "트리 변경 명령 금지" — 다음 사이클 착수 전 리더가 반영.
