# 정상화 결과서 — 2026-07-24 13:38

## 사이클 개요
- 요청: 통합 주문 관리 인라인 편집(구매상태 셀렉트 등)이 느리고 굼뜬 문제 — FE·BE 양쪽 검토 후 성능 최적화.
- 배치: D-104 단독(성능). 리더 직접 진단 + BE 조사(Explore) + FE 수정(리더) + 독립 QA(qa-verifier).

## 처리 결함
### D-104 — 통합 주문 관리 인라인 편집 성능 (FE 전체 그리드 재렌더 병목) · 표준 · **검증통과**
- 진단: BE 단건 편집 엔드포인트는 PK 단건 조회+저장으로 빠름 → 굼뜸 주범 아님. 주범은 FE 재렌더.
  - ①`columns` useMemo 매 렌더 재생성(`handleUpdate` deps=매 렌더 새 mutation 객체)
  - ②`rowData` state+`useEffect([data])` → 편집마다 500건 flatMap+setState 이중 렌더
  - ③`patchLineItemInCache`가 전 주문 객체 재생성 → 모든 행 참조 변동
  - ④행 메모이제이션 부재 → 셀 1개 편집에 수천 셀 재렌더
- 수정(frontend/src/pages/OrderGrid.tsx, 행위 보존, BE 무변경):
  1. `handleUpdate` deps → `*.mutateAsync`(RQ v5 안정참조)로 `columns` 안정화
  2. `rowData` 제거 → `processedData` 단일 useMemo로 `data`에서 직접 평탄화
  3. `rowCacheRef`로 변경 없는 주문의 행 객체 참조 재사용
  4. `patchLineItemInCache` 참조 보존(해당 주문만 새 객체)
  5. `OrderTableRow = React.memo`(비교자: row.original·isSelected·isOrderBoundary·colCount) → 편집 시 변경된 주문의 3행만 재렌더
- 수정요지: `_workspace/fixes/D-104.md`

## 판정 요약
- 독립 QA(qa-verifier) 판정: **PASS** — 행위 동등성 A~H 전항 보존(행 순서·isFirstLineItem·rowSpan·메모 스킵 안전·선택·frozen/경계선·호버·낙관패치/롤백). 판정서 `_workspace/verify/D-104_perf_verdict.md`.
- 회귀 게이트: `npx tsc -p tsconfig.app.json --noEmit` EXIT 0 · `npm run build` EXIT 0 (리더+QA 독립 2회).
- 게이트: `.autonomous` 마커 → 표준 자동 통과(판정서 PASS + 회귀 통과 충족). 커밋 진행, push는 사용자 승인 대기(.autonomous-push 부재).

## 미해결·보류·후속
- **라이브 수동 확인 권고**(FE 테스트 러너 부재로 런타임 미실행): ①구매상태 셀렉트 변경 시 타 주문 행 미재렌더 체감 ②동기화 refetch 후 선택상태·발송버튼 정합 ③다중 lineItem 주문 rowSpan 시각 무결.
- **BE 후속(P3, 미적용)**: shippingMutation 성공 시 500건 무효화는 상태전이(NEW→SHIPPED) 정확성 위해 유지. OrderLineItem.order_id 인덱스, purchaseStatus EXISTS 서브쿼리, 구매상태 액션로그 부가 2쿼리 — 측정 후 개선 여지.
- **기존 특성(이번 회귀 아님)**: `getRowId` 미설정으로 selection이 인덱스 기반 → refetch 재정렬 시 선택 의미 어긋남 잠재. 레거시부터 존재, 개선은 별도 작업(getRowId=lineItem/order id 도입 검토).

## 핵심 결정과 이유
- 리더 직접 진단 채택: 성능 결함은 재현 단위테스트가 어렵고(FE 러너 부재), 코드 논증+빌드+독립 리뷰로 게이트. defect-scout 광역 재진단 대신 대상 좁힘.
- shipping 무효화 유지: 성능보다 상태 정확성 우선(마켓 API 경로·저빈도, 다른 최적화로 refetch 렌더 비용은 이미 감소).
- 행 메모 비교자에서 `row` 인스턴스 무시: react-table Row는 매 렌더 신규 생성 → original 값 비교가 유일한 안전 스킵 기준.

## 다음 단계 참조
- D-104는 커밋 대기(FE+원장+본 결과서). **push는 사용자 승인 필요** — 승인 시 `git push origin main` 자동배포. 배포 후 위 "라이브 수동 확인 권고" 3종 체감 확인 권장.
- 성능 추가 개선 여지(다음 배치 후보): `getRowId` 도입(selection refetch 정합), BE P3 3종(측정 선행), 그리드 가상화(react-window) — 표시 행이 수천 규모로 커질 때. 현재는 memo+참조보존으로 충분.
- 원장 잔여: D-093/D-094(가격정책 재설계·사업결정 대기), D-070후보(11번가 emptyToNull 잠복), D-103(Cafe24 토큰 라이브검증 대기) 등 — 별건.
