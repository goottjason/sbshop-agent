# 2026-09-19 11:00 결과서 — D-301·D-302·D-305·D-303 배치 (주문 그리드 오매핑 + 배송상태 고착)

## 발단 (사용자 신고 2건)
1. G마켓 이정열 주문(sb_order 593)의 주소가 N스토어 김성국 주소로 저장됨. 이름·전화·통관번호·상품·우편번호는 정확.
2. G마켓 최영환 주문(579, 송장 424441786823)이 구매확정 후에도 계속 "배송중".

## 처리 결함
| 결함 | 결과 | 커밋 |
|------|------|------|
| D-301 | 그리드 `getRowId` 부재 → 재정렬 시 포커스 중 편집기 초안이 다른 주문에 커밋. 안정 행 식별자 + 편집기 key 2차 방어 | 163f9f41 |
| D-302 | 일괄 발주확인·거부·엑셀·발송이 인덱스로 `processedData`를 찾아 필터 활성 시 엉뚱한 주문 대상. `visibleData` 기준 매핑 일원화 | 163f9f41 |
| D-305 | cafe24_order_id 없는 백필 주문의 프로브 헛호출 제거(형식 가드). 상태 고착 자체는 데이터 교정으로 해소 | a5521f43 |
| D-303 | antd Button `type="button"` → `htmlType` (타입 게이트 복귀) | 2e229fae |
| D-304 | infrastructure 가격·재고 계약 테스트 6건 HEAD 실패 — 등재만 | - |

## 판정 요약
- qa-verifier PASS(FAIL 0). 수정 전 재현 9/9 실패·후 9/9 통과를 독립 복사본에서 재확인.
- 게이트: 프론트 vitest 9건·tsc 0오류·build 성공·lint 0오류. 백엔드 core 1799·api 386 실패 0. infrastructure 6건 실패는 HEAD 워크트리에서도 동일(D-304, 무관).
- 리스크: D-301/302 표준(그리드 전 컬럼·마켓 실요청 경로), D-305/303 경량.

## 핵심 실측 (재발 방지 근거)
1. **폴백 전수 검토 결론**: 백엔드 주문 도메인(Order.update 9-arg, OrderService.updateOrder, 4마켓 어댑터)에 타 주문·타 출처로 값을 대체하는 폴백은 **없다**. 오염은 프론트가 잘못된 주문 id로 PATCH를 보낸 것이고 백엔드는 요청대로 저장했다(액션 로그 17185 `ORDER_UPDATE 주문 593` 13:17:32, 1분 전 594 송장 수동입력).
2. 우편번호가 안 틀린 이유: 우편번호는 그리드에서 읽기전용 `<span>`이라 편집 경로를 타지 않는다. `OrderUpdateRequest`는 address·customsClearanceNo·message 3필드뿐.
3. 전수 조회: `address ≠ last_market_address`인 주문 10건 중 타 주문 주소로 오염된 건은 593 **1건뿐**. 나머지 9건은 동 이름 괄호 표기 차이(정상).
4. 배송상태 표시는 `sb_order_line_item.shipping_status` 단일 필드. `sb_shipment.delivery_status`는 화면 미사용(314/335 빈값, 스토어 어댑터만 채움).
5. D-300 백필 4건(579~582)은 Cafe24 원본이 없어 프로브가 영구 UNKNOWN — D-300 원장이 예측한 그대로. 근본 조치(ESM 배송현황 폴백 프로브)는 ESM 상태코드 표 미확보로 미착수.
6. 프론트 lockfile은 gitignore, Dockerfile.frontend는 `npm install` — devDependency 추가가 배포를 깨지 않는다.

## 데이터 조치
- 완료: 593 `address` ← `last_market_address`(서울 관악구 보라매로6길 26 호림 B01호) 복원, DB 재조회 확인.
- **미완(권한 정책 차단, 사용자 실행 필요)**:
  ```sql
  UPDATE sb_order_line_item SET shipping_status='CONFIRMED', updated_at=now() WHERE id=814 AND order_id=579 AND shipping_status='SHIPPED';
  ```
  580~582(라인 815·816·817)는 구매확정 신호가 DB에 없어 승격하지 않았다. 마켓에서 확인되면 같은 패턴으로 `DELIVERED`/`CONFIRMED`.
- 주의: 593 라인아이템은 09-19 01:13에 이미 DELIVERED — 실제 배송지가 어디였는지(iHerb 주문 시 어느 주소를 썼는지) 사용자 확인 필요.

## 미해결·보류
- D-304 infrastructure 6건: 어댑터 vs 계약 테스트 진실 판정 필요(최근 `482552df` 이후).
- D-305 근본: ESM+ 배송현황 폴백 프로브(표준). 선행 = ESM 상태코드 매핑표 실측.
- push 미실행(자율 push 마커 없음). 배치 미실행 확인 후 `git push origin main`.

## 다음 단계 참조
1. 사용자: 579 SQL 실행 → push → 배포 후 라이브 확인: 주문 그리드 인라인 편집(주소·메시지·통관번호·송장·구매정보·금액) 저장 1회씩, 필터 켠 상태 1건 선택→발주확인 대상 일치.
2. 다음 배치 후보: D-304(게이트 복구, P2) 단독 배치 우선 → D-305 근본(ESM 폴백 프로브) → D-299 금칙어 데이터 교정.
3. 프론트 테스트 러너가 생겼다 — 이후 프론트 결함은 vitest Red 필수. 게이트 명령: `cd frontend && npm test && npx tsc -p tsconfig.app.json --noEmit && npm run build`.
