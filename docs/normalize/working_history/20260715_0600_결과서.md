# 정상화 결과서 — 2026-07-15 06:00 · A~E 완주 (잔여 🔴 + P5/P5b/P6)

## 이번 세션 A~E 처리 총괄

| 단계 | 내용 | 커밋 |
|------|------|------|
| **A** 잔여 🔴 성능·안정 | F-SYNC-19(customs 트랜잭션 분리), F-BATCH-ST1(status distinct) | `1ef9a6f`·`1a0485b` |
| **B/P5b** 보안 최소 | F-CRED-1·7·8(시크릿 마스킹), F-MISC-17·7(접근제어 옵트인) | `019e20d`·`97b5b79` |
| **C** SP-3 상품 | F-PSRC-2·6(소싱/bulk 부분실패), F-PROD-12·16(이미지 부분실패) | `139a581`·`fee0baa` |
| **D/P5** 구조 리팩토링 | SP-9(Supplier·MarketRegistration 서비스층), SP-11 부분(발주batch·root-cause) | `d81fa42`·`04062a9` |
| **E/P6** 응답 DTO | SP-5 전체 스윗: order 쓰기·product·supplier·batch·sync·mreg 엔티티→DTO(계약보존) | `c2b4e47`·`5ff8890`·`d556697`·`54087b6` |

- **TDD·회귀·프론트게이트 전건 통과**. 병렬 fixer 다수(격리 worktree로 상호간섭 회피). 원장 무단수정 되돌림(추적은 api-analysis 단일원본).
- **E 계약보존 전략**: DTO를 현재 직렬화 JSON과 동일 필드로 미러 → 프론트 무변경(tsc 확인, 바이트동일 계약 테스트). 도메인/API 결합·지연로딩 유출만 차단.
- **A~D 표준/중대 혼재**, E 표준(구조). 미푸시(사용자 일괄 push 예정).

## 시스템성 패턴 최종
- ✅ **완료**: SP-1(비동기 진실성), SP-2(영속상태·SSE cross-JVM), SP-3(부분실패), SP-4(상태가드), SP-5(응답 DTO), SP-6(marketType), SP-9(서비스층), SP-10 축소(보안최소)
- 🔶 **부분**: SP-7(bad-enum·404 일부), SP-11(발주batch·root-cause 완료; 마켓sync골격·이미지3경로·batch트리거 후속)
- ⛔ **보류**: SP-8(필드삭제 시맨틱 — 제품결정)

## 결함 집계 (FINDINGS-CHECKLIST 기준)
- 해결 54 · 부분/오탐 7 · 미해결 ~146(대부분 저순위 GAP/SMELL/NOTE)
- **🔴 P0 거의 전부 해소**. 잔여 개별결함은 저순위(F-ORD-2 기간필터, F-ORD-8 예외유형 등)와 후속 리팩토링(SP-11 잔여).

## 미해결·후속 후보
- **SP-11 잔여**: 마켓 sync 4서비스 골격(F-SYNC-5), 이미지 3경로(F-PROD-15·19·20), batch 트리거 4종(F-BATCH-3), F-CAFE-12.
- **개별**: F-PSRC-9(SB코드 결번), F-PSRC-13(게시 멱등성), F-ORD-2·5·8, F-BATCH-M2/M3, email 완전 멱등성, ProductSync 상품별 크롤예외, getAllBatches 죽은코드, SP-8.
- **SP-10 잔여**(범위 밖): CORS 전역, preview PII, SSE 상한/인증, CAFE.

## 배포 유의 (일괄 push 시)
- ddl-auto=update 첫 기동(sb_market_sync_status 자동생성) 로그 확인.
- 도는 배치 중단 가능([[deploy-interrupts-running-batch]]).
- 라이브 검증: P4d SSE 왕복, P4a sync상태 UI, P2 프론트 400표시, F-CRED Cafe24 재인증 Client ID.
