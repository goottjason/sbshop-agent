# 2026-09-05 19:50 결과서 — D-295·D-296·D-298·D-299 배치 (11번가 묶음 + 카페24 병렬)

## 처리 결함
| 결함 | 결과 | 커밋 |
|------|------|------|
| D-296 | 11번가 상세설명을 전용 부분수정 API(updateProductDetailCont)로 분리, 전체 XML은 이미지 전용·htmlDetail 치환 제거 | 9912fa93 |
| D-295 | 카페24 등록 brand_code(리졸버)·원산지 조합(Cafe24OriginResolver)·무게 kg·태그 하위 리소스 배선 | 7f618fc6 |
| D-298 | 고시 블록 타깃 재시도 골격 — 0500 거부 시에만 주입·1회 재PUT, 코드표 전 완전 무동작 | ec9e3566 |
| D-295/296 후속 | 라이브 실측 교정 — 11번가 성공 봉투 resultCode 000 정확판정, 카페24 원산지 배열명 매칭 | (실측 커밋) |
| D-299 | 부분 — 상세설명 경로 노출 제거. 금칙어 15건은 브랜드 경로(전체 XML)라 미해소, 상품명 데이터 교정 별도 필요 | - |

## 판정 요약
- 게이트: 전 모듈 2,038건(교정 후 인프라 461) 0 실패, compileJava/TestJava 그린. 2회 배포 모두 기동 확인(10:41·10:48 UTC).
- 리더 판정(중대): D-298 주입 방식은 **타깃 재시도** — 무조건 주입은 정상 1,681건의 마켓 고시를 플레이스홀더로 덮는 표시 품질 저하라서 기각.

## 핵심 실측 (재발 방지 근거)
1. 11번가 `GET prodmarket/{prdNo}` 응답에는 ProductNotification 이 **원래 없다** (실패·성공 상품 공히, 태그 diff 0). "받은 블록을 되돌려 거부"라는 최초 가설은 반증. 82건을 가르는 건 마켓측 저장 고시 스냅샷.
2. 11번가 상세설명 전용 API 성공 봉투 = `<ProductDetailCont><resultCode>000</resultCode>` (PDF 유추 `<Product/>` 오류). 부분문자열 판정 금지 원칙 재확인.
3. 카페24 `GET /admin/origin` 래퍼 키는 `origin`, `origin_place_name` 은 배열 `["아메리카","미국"]`. 문자열 비교는 전량 기타(1800) 폴백이었음.
4. D-294 2단계 모달 검증 중 좌표 클릭이 마스크에 떨어져 "닫힘 결함"으로 오인 — 브라우저 자동화는 JS 직접 클릭이 안전.

## 미해결·보류
- **D-298 잔여**: 고시 유형·항목 코드표(셀러오피스 팝업 — 세션 브라우저 안전차단) 확보 시 표 채움 + 가드 3개 계약 뒤집기 + 82건 중 1건 라이브. 사용자 캡처 요청 중.
- **D-299 잔여**: 금칙어 15건 상품명 교정(영문 브랜드 제거) 데이터 작업.
- **D-295 잔여**: 신규 등록 1건 라이브 대조(brand_code·product_weight·태그 실반영).
- 파생 백로그: 카페24 syncProductFields 무게·태그 미지원(MarketEditField 확장 후보), DraftReviewPage.tsx:263 weightG 표기(kg값을 g로 표시), ProductManageUseCase.republishToMarkets 이미지·상세 분리 호출 경로.

## 다음 단계 참조
1. 사용자 코드표 캡처 도착 → fixer-11st 이름으로 재개(트랜스크립트 유지) 또는 새 fixer 로 표 채움 사이클.
2. D-295 신규 등록 라이브 대조는 다음 자연 등록 1건에서 로그로 확인 (원산지 해석·태그 POST 로그 추가돼 있음).
3. 다음 배치 후보: D-299 금칙어 15건 데이터 교정(단순·저위험), 카페24 무게·태그 수정경로 확장, D-240 스케줄러 활성화(사용자 승인 필요).
