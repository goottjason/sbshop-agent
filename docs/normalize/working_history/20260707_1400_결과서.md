# 사이클 5 결과서 — 2026-07-07 (프론트 배치)

## 처리 결함 (2건 검증통과 → 커밋)

| 결함 | 등급 | 처리 | 커밋 |
|------|------|------|------|
| D-008 | P3/경량 | orderApi 미호출 함수 3건 삭제 (백엔드 엔드포인트 부재 교차 확인) | 35de74b |
| D-007 | P3/표준 | lint 58→6 problems (에러 56→4). 타입 계층만 수정 — 런타임 불변. 잔여 6건은 로직 재구성 필요분으로 사유와 함께 보류 | dd481ca |

## 판정 요약

- 내부 QA 2/2 PASS. 리더 게이트: lint 6 problems(기준선 대비 -52) + tsc-app 5 errors(기준선 8의 부분집합, 신규 0 = 무회귀) + build 그린 + 백엔드 무접촉.
- **게이트 자체의 결함 발견·교정**: 루트 `npx tsc --noEmit`은 references-only tsconfig라 검사 0건 헛-그린이었음 → 실 게이트 `npx tsc --noEmit -p tsconfig.app.json`으로 스킬 3개 교정 (커밋 80998f3). 이전 사이클들의 "tsc 그린" 근거 중 프론트 부분은 이 헛-그린이었음을 소급 인지 — 단 해당 사이클들의 프론트 변경은 D-008(삭제)뿐이라 판정 유효성에 영향 없음(실 게이트로 재확인됨).

## 신규 결함 (원장 등재)

- **D-017 (P2)**: OrderGrid `updateOrderLineItem` 미정의 — 소싱금액/물류비/유니패스 체크박스 인라인 편집 시 ReferenceError. 백엔드 엔드포인트는 존재, 프론트 API 래퍼 누락. vite build가 타입체크를 안 해 잠복. (fixer·verifier 독립 교차 확인)
- **D-018 (P2)**: 배송 모달 `carrier` ↔ 백엔드 `shippingCarrier` 필드명 불일치 — 택배사 저장 누락 개연. 전형적 경계면 결함.

## 미해결·보류

- 미해결: D-015/D-016(자족성 — 중대 설계 필요), D-017/D-018(경계면 배치 후보), D-007 잔여 lint 6건(하위항목), api AsyncConfig 껍데기
- 보류: D-002(오판)

## 다음 단계 참조

- **사이클 6(자율 가능, 표준)**: D-017+D-018 경계면 배치 — 둘 다 "프론트↔백엔드 계약 불일치"로 integration-qa 체크리스트의 정중앙. D-017은 updateOrderLineItem 래퍼 구현(+세 입력 경로 검증), D-018은 필드명 정합(백엔드 DTO 기준 권장). 테스트 러너 부재로 수동 검증 절차 명시 필수 — vitest 도입 검토 가치 상승(신규 의존성 — 사용자 승인 필요).
- **사이클 7(중대 — 설계안 먼저)**: D-015+D-016 마이그레이션 자족성 배치.
- push는 사용자 지시 대기 (자율 마커는 push 미포함).
