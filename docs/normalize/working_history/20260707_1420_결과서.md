# 사이클 6 결과서 — 2026-07-07 (경계면 배치)

## 처리 결함 (2건 검증통과 → 커밋 31796eb)

| 결함 | 등급 | 처리 |
|------|------|------|
| D-017 | P2/표준 | updateOrderLineItem 미정의 해소 — **필드별 라우팅** (isUnipassDone→범용 PATCH, 소싱금액·물류비→/sourcing). 검증자 사전 대조가 "범용 1벌 래퍼 = 2필드 무성 유실" 함정을 차단 |
| D-018 | P2/표준 | carrier→shippingCarrier 키 정합 + 택배사 옵션을 백엔드 enum 7종과 1:1 정렬 (키만 고쳤다면 enum 역직렬화 실패가 남을 뻔) |

## 판정 요약

- 내부 QA 2/2 PASS, 반려 0. 리더 게이트: tsc-app 5→4 errors(TS2304 소멸, 신규 0), lint 무회귀(6), build 그린, 백엔드 무접촉.
- null-보존 독립 검증: 단독 필드 전송이 기존 SourcingData를 훼손하지 않음 (`toSourcingData(existing)` toBuilder 패턴, OrderService:265,273).
- **미검증 영역**: 런타임 실검증 (테스트 러너·백엔드 실행 환경 부재) — fix.md에 수동 절차 명시. 사용자 수동 확인 권고: 주문 그리드에서 소싱금액/물류비/유니패스 편집 저장, 배송 모달 택배사 저장.

## 신규 후보 (원장 기록, 제품 결정 필요)

- 인라인 소싱 편집 뮤테이션 onError 토스트 부재 — 실패가 조용히 넘어가는 UX 결함
- 국제 택배사(DHL/FedEx 등) 백엔드 enum 미표현 — 국제 배송 쓰는지 제품 판단 필요

## 미해결·보류

- **중대 대기**: D-015+D-016 마이그레이션 자족성 (설계안 → 사용자 승인 필요)
- 하위/후보: D-007 잔여 lint 6건, api AsyncConfig 껍데기, 위 신규 후보 2건
- 보류: D-002(오판)

## 다음 단계 참조

- 표준 등급 자율 배치는 소진 — 다음 작업은 모두 사용자 입력 필요:
  ① D-015+D-016 자족성 배치 (baseline 전략 설계안 작성 → 중대 승인)
  ② vitest 도입 여부 (프론트 회귀 안전망 — 신규 의존성)
  ③ 신규 후보 2건의 제품 결정
  ④ push / 배포 (배포 시 사이클 4 결과서의 배포 체크리스트 필수 — 운영 DB 스키마 실측, Flyway V5 적용 확인)
- 배포 전 수동 확인 권장: 이번 배치의 수동 검증 절차 (라인아이템 편집·배송 저장).
