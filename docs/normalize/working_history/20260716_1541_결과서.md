# 정상화 결과서 — 2026-07-16 15:41 · R2 관측성·활동로그 (병렬 5)

REFACTORING-PLAN 권장 순서대로 R5·재무버그·Cafe24스킵에 이어 **R2(관측성·활동로그)**를 병렬 tdd-fixer 5명으로 처리했다. 파일 비중첩 → 병렬, 리더 단독 회귀 게이트 후 관심사별 커밋. push 대기.

## 이번 사이클 처리

### 완료 (관측성 — 로그 기록 추가)
| 결함 | 처리 | 커밋 |
|------|------|------|
| F-ORD-5·15 | 발주확인/취소 **단건** 실패 활동로그 marketType 채움(id 재조회, `OrderService.marketTypeOfOrder`). bulk는 다마켓 null 유지(의도). | `4006db3` |
| F-SYNC-3 | 비동기 sync 5개 트리거(쿠팡·스토어·11번가·ESM·쿠팡정산)에 SUCCESS/FAILED 대칭 기록. 응답계약(200/500) 불변. | `8a572b2` |
| F-MISC-19 | 수동 이메일 트리거도 SyncStatus(markRunning→Completed/Failed) 기록. `SyncMarketKeys.EMAIL` 추가(값 불변, /orders/sync/status 계약 무영향). | `10b4eb2` |
| F-SUP-3·CS-5·UC-5 | 공급사/환율 생성 활동로그 기록(SUPPLIER_CREATE·CURRENCY_CREATE). 엔티티노출은 기존 Response DTO로 기해결. | `ec3c802` |

### 오탐/유지 (코드 무변경)
- **F-BATCH-7**: 완료(SUCCESS/FAILED)는 컨트롤러가 아니라 `BatchCompletedEvent → ActionLogBatchListener`가 기록(SP-F에서 해소). marketType=null은 다마켓 배치라 의도. → 이미 해소.

### 사용자 정책 확정 (조회 로그)
- **F-ORD-4·F-PROD-3·F-CRED-3** (조회 API 활동로그 미기록): 사용자 결정 **"조회는 로그 미기록"**(변경만 기록, 로그노이즈·테이블비대 방지)으로 확정. 코드 변경 없이 "의도/유지".

## 판정 요약
- 리스크: R2는 전부 **표준 이하**(관측성 추가, 응답/계약 보존). 병렬 fixer는 격리(worktree/단독 컴파일)로 mid-edit 경합 회피, 리더 단독 `./gradlew test` 전건 SUCCESSFUL(2m17s).
- 커밋: 관심사별 4 + docs 부기 1. 신규 테스트 총 23건(F-ORD 6·SYNC-3 10·MISC-19 3·SUP 4).

## 다음 단계 참조
- **다음 배치 권고: R1(오류·HTTP 시맨틱)** — 미존재→404(ResourceNotFoundException+GlobalExceptionHandler), 예외유형 보존(F-ORD-8), 부분/전건 실패 표면화(F-MISC-20·ORD-12·33). **프론트 계약 영향** 있어 도메인별 사용자 승인 동반(R1은 표준~중대 경계). 프론트가 상태코드/바디에 의존하는지 병행 확인 필요.
- 그 다음 R6(조회 품질, 낮) → R3(정합, 높) → R4(sync 엣지, 높·라이브 확인) → R7(보안 선택) → R8(문서).
- quick-win 후보(잔여): 변경감지 스킵 쿠팡 확장(판매재개 반복실패 완화).
- **배포 유의**: R2는 로그만 추가·계약 보존 → 프론트 영향 없음. 정산 로그·Cafe24 스킵 로그와 함께 관측성이 향상됨. 일괄 push 시 신규 유의사항 없음.
