# 정상화 결과서 — 2026-07-18 01:01 (KST)

## 처리 대상

**신고:** 통합주문관리 상단 동기화 상태가 "9시간 전"으로 정체(30분 주기여야 함). 스케줄러 미동작 의심 + 최근 api·worker 단일 JVM 병합이 원인일 가능성.

## 진단 결론 — 표시 버그 (동기화·스케줄러는 정상)

라이브 검증:
- 컨테이너 TZ=UTC, 병합 앱(단일 JVM) `scheduling-1` 스레드에서 스케줄러 **정상 실행 중** (15:15 스마트스토어·15:20 11번가·15:30 이메일 등, 30분 스태거 그대로). 병합은 스케줄링을 깨지 않았음.
- `sb_market_sync_status.last_sync_at`은 **UTC 벽시계값**(`LocalDateTime.now()`, 컨테이너 UTC). 실제 몇 분 전.
- **근본원인:** 프론트 `OrderGrid.timeAgo`가 zone 없는 naive 문자열을 `new Date()`로 파싱 → 브라우저 로컬(KST)로 오해석 → UTC↔KST **정확히 9시간** 오차. 같은 `timeAgo`를 재고현황 셀(`product.updatedAt`)도 공유해 동일 증상.

## 수정 (커밋 `6519cc0`)

| 구분 | 내용 | 등급 |
|------|------|------|
| 프론트 | `timeAgo`가 `utils/datetime.toKstDate`(naive→UTC 파싱) 사용 → 동기화 바 + 재고현황 동시 해소 | 경량 |
| 백엔드 | 3개 스케줄러 9개 `@Scheduled`에 `zone="Asia/Seoul"` — 일/시 잡(정산 "새벽 2시" 등)이 UTC로 9h 밀려 실행되던 것 교정. 타임스탬프는 UTC 벽시계 유지 | 표준 |
| 테스트 | `SmartStoreOrderFetchFailureTest`가 KST 자정~오전9시에 어댑터 UTC 윈도잉과 어긋나 실패하던 기존 플래키 → UTC 날짜 사용 | 경량 |

**명시적 비대상:** `orderDate`(OrderGrid:802)는 마켓 API `+09:00` 파싱(KST 벽시계)이라 raw `new Date`가 맞음 → 미변경. `ProcessStatusPage`(formatKst)는 이미 정상.

## 판정 · 배포

- 프론트 tsc/build + 백엔드 전체 회귀 통과(EXIT=0). 배포 success, api·frontend 재기동, health 200.

## 미해결 · 다음 단계 참조

- **근본 부채:** 백엔드 시각 필드의 타임존 의미가 혼재 — `LocalDateTime.now()`계열=UTC 벽시계 vs 마켓 API 파싱(orderDate)=KST 벽시계. 프론트 `datetime.ts`는 "전부 UTC 벽시계" 전제라 orderDate엔 안 맞는다(현재는 orderDate를 raw parse로 우회). 장기적으로 **timestamp 타입 일원화**(Instant/OffsetDateTime + 명시 오프셋 직렬화, 또는 컨테이너 TZ 정책 확정) 리팩토링 후보. 지금은 증상별 정확 교정만 수행.
- 라이브 최종 확인: 브라우저 새로고침 시 동기화 바·재고현황이 "방금/N분 전"으로 표시되는지, 크론이 KST :00/:30에 도는지(로그) 확인 권장.
- 이 결과서는 재배포 방지 위해 로컬 커밋만; 다음 코드 변경 push에 동반.
