# 정상화 결과서 — 2026-07-16 16:30 · R6 조회 품질·조회계 입력검증 (병렬 7)

R5·재무·Cafe24스킵·R2·R1에 이어 **R6(조회 품질 — 정렬·페이징·필터·조회계 입력검증)**를 병렬 7에이전트로 처리했다. 파일 비중첩 병렬, 리더 단독 회귀 게이트 후 관심사별 커밋. push 대기.

## 처리 결과

### 완료
| 결함 | 처리 | 커밋 |
|------|------|------|
| F-PROD-1 | marketFilter+keyword 배타→AND 결합(keyword 무시 버그 해소, 리포지토리 결합쿼리) | `fe4d630` |
| F-PROD-18 | 크롤 이미지 URL http(s) 형식검증+순서보존 중복제거 | `fe4d630` |
| F-PROD-22 | 크롤 이미지 MAX 30 절단+log.warn(소스값이라 거부 아닌 절단) | `fe4d630` |
| F-BATCH-ST2 | 최신순 정렬(R1 기구현) 회귀고정. 목록형태는 프론트 소비처0라 미확장 | `dffe900` |
| F-BATCH-S3 | getBatchStatus 선택적 status 필터(비파괴), 404 동작 보존. offset 페이징 보류 | `dffe900` |
| F-SUP-4·LC-2 | 공급사/통화 목록 supplierCode·currencyCode 정렬(OrderBy). 페이징 보류(소량 마스터) | `c4c253d` |
| F-MISC-1 | action-logs recentLogs(page,size) 오프셋 페이징(응답 평면배열 유지, 비파괴) | `3b749dc` |
| F-MISC-2 | 컨트롤러 DEFAULT/MAX_LIMIT·page 하한 명시 방어 | `3b749dc` |
| F-PSRC-5 | iHerb URL 형식 정규식·MAX 100·중복제거(잘못된 URL 조용한실패→400) | `81630a6` |
| F-S4 | 소싱 금액 음수 검증(0 허용·null 통과) | `68a0920` |

### 오탐/유지 (코드 무변경)
- **F-ORD-3** (shippingStatuses EXISTS): 배송상태는 라인아이템 속성 → EXISTS는 "해당상태 라인 있는 주문"을 fan-out·카운트왜곡 없이 매칭. 변경 시 혼합상태 주문 누락(운영사고). 프론트 rowSpan 렌더와 정합. **유지**(변경은 필터 재정의 요청이지 버그 아님).

## 판정 요약
- 계약 안전: 전 항목 프론트 사용처 검증 후 비파괴(응답형태 유지, 파라미터는 옵셔널 추가, 정렬만). 프론트 코드 변경 0.
  - 특기: `/status` 배치목록·action-logs·공급사/통화는 프론트가 **평면 배열**을 소비 → Page envelope 대신 옵셔널 파라미터로 페이징/필터, 형태 보존.
- 게이트: 리더 단독 `./gradlew test` 전건 SUCCESSFUL(2m19s). 병렬 7fixer는 격리 worktree로 mid-edit 경합 회피(공유트리 일시 컴파일불가는 격리검증으로 우회, 합류 후 정상).
- 커밋: 관심사별 6 + docs. 신규 테스트 다수(필터결합·URL검증·정렬·페이징·금액검증).

## 다음 단계 참조
- **다음 배치 권고: R3(트랜잭션·멱등성·동시성, ★★★ 리스크 높)** — 긴 트랜잭션 내 외부 I/O 분리(F-PSRC-8), 부분성공 건별 tx(F-ORD-31), 게시 멱등성(F-PSRC-13·9), 동시배치 advisory lock(F-BATCH-1·SYNC-17·CAFE-6), 하드삭제 정합(F-PROD-27·28). **각 건 재현·정합 테스트 필수, 분산정합이라 신중**(건별 순차 권장, 대량 병렬 지양).
- 그 다음 R4(sync 엣지, 높·라이브 확인 동반) → R7(보안 선택) → R8(문서).
- quick-win: 변경감지 스킵 쿠팡 확장.
- **배포 유의**: R6는 조회 개선·입력검증 추가로 응답형태 불변(비파괴). 신규 400 케이스(잘못된 iHerb URL·소싱 음수금액)만 프론트 표시 확인 권장. 누적 대기 커밋 다수 — 배포 시 일괄. **R2·R1·R6 미배포 상태.**
