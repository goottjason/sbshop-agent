# 2026-07-20 10:28 결과서 — D-088 옥션/G마켓 N10 신규주문 "구매준비" 오분류 수정

## 개요
사용자 신고: 옥션 주문 2566278285(서종수)이 실제로는 **신규주문(주문확인 전)**인데 시스템이 **"구매준비(PREPARING)"**로 표시. 라이브 확증으로 원본 Cafe24 `order_status`=N10임을 확인하고, `Cafe24OrderSyncService.mapStatus()`의 N10 오분류를 TDD로 수정했다. 관련 [[state-guard-case-by-case]] · [[product-dispatched-status-plan]].

## 근본원인 (D-088)
`mapStatus()`가 `case "N10","N20","N21","N22" -> PREPARING`으로 **N10(상품준비중)을 발주확인 후 상태에 잘못 포함**. 그러나 같은 코드베이스의 발주확인 액션 `Cafe24OrderApiClient.java:67` `ACCEPT_STATUS="N20"`이 발주확인의 *결과*를 N20으로 정의 → **N10=발주확인 전(신규주문)**이 논리적 필연. 옥션/G마켓은 결제완료로 유입되어 Cafe24가 신규건에 N10을 부여하므로, NEW여야 할 건이 PREPARING으로 표시됨. 기존 테스트(`Cafe24OrderSyncServiceTest`)와 코드 주석이 "N10=발주확인 후"로 잘못 가정하고 있었다.

## 라이브 확증 (2026-07-20, read-only)
- DB: 주문 196 = AUCTION, market_order_no 2566278285, 서종수, cafe24_order_id 20260719-0000018, 라인아이템 427 shipping_status=**PREPARING**(신고 일치).
- `/api/v1/orders/sync/cafe24/preview`(컨테이너 내부 호출) 원본 응답:
  | cafe24_order_id | market_order_no | 마켓 | 원본 order_status | 매핑(수정 전→후) |
  |---|---|---|---|---|
  | 20260719-0000018 | 2566278285 | auction | **N10** | PREPARING → **NEW** |
  | 20260715-0000010 | 4469254653 | gmarket | N20 | PREPARING (불변) |
  | 20260716-0000016 | 4469438260 | gmarket | N30 | SHIPPED (불변) |
- N10/N20/N30 스펙트럼 실재 확인 → 오분류 100% 확정.

## 처리 내용
| 구분 | 내용 |
|------|------|
| fix(core) | `Cafe24OrderSyncService.mapStatus()`: `N00/N02/N10 → NEW`(발주확인 전), `N20/N21/N22 → PREPARING`(발주확인 후)로 분리. N10을 NEW 그룹으로 이동. |
| test | `Cafe24OrderSyncServiceTest`: (a) `mapsGmarketAndSkipsOthers` N10 기대값 PREPARING→NEW 정정, (b) `mapsStatusCodes`→`n10New`로 재작성(N10=NEW), (c) N21/N22 PREPARING 회귀 추가, (d) 갱신경로 `updatesExistingFoundByMarketOrderNo` N10→NEW 정정. |

## 게이트
- 타겟 `Cafe24OrderSyncServiceTest` Green.
- **회귀 전체 `./gradlew test` BUILD SUCCESSFUL** (core/api/worker/infrastructure, 2m19s).
- 리스크 등급 표준(마켓 주문상태 계약, 단일 core 파일) — `.autonomous` 마커로 승인 자동 통과. push(배포)는 `.autonomous-push` 부재로 사용자 대기.

## 데이터 교정
- 별도 수동 DB 수정 불필요: 갱신경로(`persistOrder`)가 재동기화 때 mapStatus를 재적용하므로, **배포 후 다음 esmplus 동기화(매시 10·40분)에서 주문 196이 N10→NEW로 자동 교정**된다. (운영자가 옥션에서 발주확인하면 이후 N20→PREPARING으로 정상 전이.)

## 다음 단계 참조
- **push 대기 중** — 커밋은 로컬 완료. `git push origin main` 시 웹훅 자동배포(api 재시작). 배포는 도는 배치 중단하므로 [[deploy-interrupts-running-batch]] 확인 후 push 권장.
- 배포 후 확증: `/api/v1/orders/sync/cafe24/preview` 또는 esmplus 동기화 트리거 후 DB에서 주문 196 shipping_status=NEW 확인.
- `Cafe24OrderApiClient.java:66` "상태코드 라이브 검증 대상" 주석 — N20(발주확인 타깃)·N40/N50(배송완료/구매확정) 값은 이번에 N10/N20/N30만 라이브 확인됨. N40/N50/C·R·E 계열은 미확인이므로 향후 실주문 발생 시 대조 권장.
