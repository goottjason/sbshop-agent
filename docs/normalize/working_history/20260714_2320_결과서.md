# 정상화 결과서 — 2026-07-14 23:20 · SP Phase 3 (SP-3 부분실패 표면화)

## 이번 사이클 처리 — Phase 3 (order 도메인)

| 항목 | 대상 | 처리 | 커밋 |
|------|------|------|------|
| 3-1 | 일괄발송 부분실패 (F-ORD-30, 🔴) | `BulkShipResult`(성공/실패/스킵) 신설, `bulkShipOrders`가 마켓 실패 삼키지 않고 집계, `/ship` 응답 교체, 결과기반 로그 | `ffdaed3`·`dbfefec` |
| 3-2 | 일괄 발주확인/취소 로그 (F-ORD-9·17) | 컨트롤러가 `failedCount` 기반 SUCCESS/FAILED 기록(서비스는 이미 정상 집계) | `ffdaed3` |
| 3-3 | manual-update-all 길이 가드 (F-BATCH-A2) | productIds/commands 불일치 → 400 | `6095f1b` |
| 프론트 | /ship 결과 표시 | 발주확인과 동일 3분기 토스트(성공/부분/실패 + 실패주문·사유·스킵) | `dbfefec` |

- **TDD**: 신규 Red 3(OrderShipServiceResultTest·OrderControllerBulkResultLogTest·BatchControllerManualUpdateAllValidationTest). 전 모듈 `:core:test :api:test` BUILD SUCCESSFUL(실패 0). 프론트 tsc/build 통과.
- **리스크 중대**(마켓 인접 + 응답 스키마 변경) → 사용자 승인 후 커밋. **push 미실행**.

## 핵심 결정과 이유
- **BulkShipResult 필드를 BulkConfirmResult와 일치**시켜 프론트가 발주확인과 동일 컴포넌트/패턴으로 표시 → 재사용·일관성.
- **주문단위 집계 규칙**: 한 주문에서 하나라도 마켓 전송 실패 시 그 주문을 failed로(부분성공도 실패로 표면화). 발송 라인이 하나도 없던 주문(전부 이미발송/송장없음)은 skipped로 분리 — 실패와 정상 스킵 구분.
- **라이브 계약 불일치 발견·해소**: 프론트는 이미 BulkShipResult 필드를 참조 중이었는데 백엔드가 Order[]를 줘 전 필드 undefined 상태였음 → 이번에 정합.

## 미해결·잔존 (SP-3 상품 도메인 후속)
- F-PSRC-2/6(iHerb·bulk 등록 부분실패 응답 미반영), F-PROD-12/16(이미지 개별 실패 조용히 누락) — **상품 도메인 SP-3 후속 배치**.
- F-SYNC-3(비동기 sync 로그) — P4(비동기)와 함께.

## 다음 단계 참조
- **다음 배치 = Phase 4 (SP-1 비동기 진실성 + SP-2 영속 상태)** — 최대 규모·중대.
  - SP-1: `@Async`/`new Thread` 실패가 200 성공으로 은폐(F-SYNC-2·23, F-MISC-8, F-BATCH-2). 스케줄러 markCompleted가 실제 완료와 무관(F-SYNC-23).
  - SP-2: SyncStatus/batch status/SSE가 JVM 로컬 인메모리 → 멀티-JVM(api+worker)서 상태 UI 무력화(F-SYNC-1·25, F-MISC-16, F-BATCH-2). DB+advisory lock으로 이전 필요([[deployment-two-jvm-topology]]).
  - **선결 설계결정**: (a) 비동기 작업 상태/실패를 어디에 기록(DB 테이블? 기존 process_status 확장?), (b) 스키마 수동관리 체제라 신규 테이블/컬럼은 수동 DDL 필요 — 사용자 확인. (c) SSE cross-JVM은 별도 난이도(worker 이벤트를 api로) — 범위 분리 검토.
  - 리스크 **중대** + 스키마 변경 가능 → 착수 전 설계 승인 권장.
