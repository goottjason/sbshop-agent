# 정상화 사이클 결과서 — 2026-07-25 13:47

## 처리 결함
- **D-106**: 상품 상세 모달 필드 편집이 서버에 저장 안 됨 (소스URL 등) — **수정완료·검증통과·커밋(6fcb3f8)**

## 진단 요약
사용자 신고: "상품 관리 상세 모달에서 소스 URL을 바꿔도 안 바뀐다."

경계면 대조로 근본원인 확정:
- **FE (문제 지점)**: `ProductDetailModal.handleSave`가 판매가만 실제 `updatePriceStock`로 저장하고, **소스URL 포함 나머지 전체 편집 필드는 `productMockApi.updateProductFields`(모킹 — 300ms 지연 후 성공만 반환, 서버 미도달)로 처리**. 저장 토스트만 뜨고 실제 반영 없음, 모달 닫으면 값 소실.
- **BE (정상)**: `PUT /api/v1/products/{id}` (`ProductController.updateProduct` → `ProductUpdateCommand` → `Product.updateSourcingInfo`가 `sourceUrl` 반영)가 이미 존재. FE 클라이언트 `productApi.updateProduct`도 이미 존재. **모달만 연결 안 됨** — product-grid 리팩토링(2026-07-22) 때 남긴 "다음 세션 백엔드 TODO" 모킹이 백엔드 완비 후에도 잔존.

## 수정
`frontend/src/pages/product/ProductDetailModal.tsx`:
- `handleSave`가 전체 편집 필드를 실제 `productApi.updateProduct(id, body)`로 저장하도록 배선.
- 평탄 폼 `Fields` → `ProductUpdateRequest` 매핑(유일 차이 `productName`→`name`, 나머지 필드명 동일).
- 판매가 마켓 동기화(`updatePriceStock`) 경로는 유지(DB+연동마켓 가격 반영).
- `productMockApi.updateProductFields` import·사용 제거(파일은 `bulkDeleteProducts` 때문에 유지).

## 판정 요약
- 리스크 등급: **표준**(FE 배선 변경). 자율 마커(`_workspace/.autonomous`) 존재 → 표준 자동 승인.
- FE 회귀 게이트: `tsc -p tsconfig.app.json --noEmit` EXIT 0, `npm run build` PASS.
- enum 라운드트립 검증: ProductCategory/VendorType/MeasureUnit 모두 `@JsonValue`/`@JsonCreator` 부재 → `name()` 대칭 직렬화. 상세 응답이 내려준 enum명을 그대로 되돌려 역직렬화하므로 미변경 필드 안전.
- 백엔드 PUT 경로는 기존 테스트로 커버(수정 없음).

## 미해결·후속(별건, 본 결함 범위 밖)
- 카테고리·소싱처·단위 입력이 **자유 텍스트 인풋** → 잘못된 enum명 입력 시 저장 400 가능(모킹 시절엔 검증 안 됐음). 드롭다운化 필요 — 후속 UI 과제.
- 상세 모달의 `detailHtml`은 읽기전용(iframe)이나 저장 body에 원값 그대로 재전송됨(동일값 재기록, 무해).

## 다음 단계 참조
- **6fcb3f8 push 대기** — main push 시 자동배포로 api 재시작. 배포 후 라이브 확인: 상품 상세 모달에서 소스URL 편집→저장→모달 재오픈 시 **값 유지** 확인(모킹 시절엔 소실). 브랜드·소싱처·메모 등 타 필드도 동일 확인.
- FE 테스트 러너(vitest 등) 부재 상태 지속 — 이런 FE 배선 회귀는 자동 재현 불가, 빌드+코드논증으로 대체 중. 러너 도입은 별도 개선 과제(D-104·D-106 공통 한계).
- 잔여 백로그: 자유텍스트 enum 필드 드롭다운化(D-106 후속), 그 외 D-093/094(가격정책) 등은 기존대로.
