# 2026-09-20 07:25 결과서 — 배포 3단계(자동 롤백) 완료, lockfile 커밋, 서버 정리

## 처리
| 항목 | 내용 | 커밋 | 상태 |
|------|------|------|------|
| D-320 | 프런트·루트 `package-lock.json` gitignore 해제·커밋, Docker·CI `npm ci` | da870fe2 | CI·배포 통과 |
| 서버 정리 | 옛 릴리스 복사본 10개(750MB) 삭제, Docker 잔재 정리, 민감 덤프 3개 권한 700/600 | (서버 작업) | 완료 |
| D-321 | 교체 뒤 점검 확대(api 헬스·nginx 라우팅·scraper 헬스) + 자동 롤백(종료코드 7·8) | bbc0268b, 228cc062, 후속 | 검증 PASS, 실서버 실패 주입 통과 |

## 판정 요약
- 검증자 재검토 5: 조건부 PASS → 지적 3건(재시도 루프 단언·롤백 예산·frontend 단독 경로) 반영.
- 게이트: 셸 테스트 336, 변형 25건 사망. 파이프라인(CI 테스트 ∥ 이미지 빌드 → 서버 pull·교체) 통과.
- 실서버: `recreate` 로 세 서비스의 새 점검 오탐 없음 확인, **실패 주입으로 자동 롤백 실증**(종료코드 7, 53초, 원래 이미지·지문 복구).

## 핵심 결정과 이유
- 무중단(블루/그린)은 하지 않음 — api 는 스케줄러 26개가 도는 단일 JVM 이고 프로세스 간 락은 토큰 갱신뿐이라 두 인스턴스 동시 기동은 중복 실행 위험. 스케줄러 리더 선출(코드 변경)이 선행돼야 한다.
- 이번 실행에서 교체한 서비스를 전부 되돌림 — api 만 새 버전·frontend 만 옛 버전 같은 혼합 상태 방지.
- 라우팅 점검을 nginx 컨테이너 안에서 사용자 경로로 — 옛 IP 로 인한 502 를 종단에서 잡는다. 200·301·302·401·403 통과(인증 흐름 변경에 둔감).

## 미해결·보류
- 사용자 결정: `~/projects/sbshop-export`(295MB, 마켓 인증정보·주문 평문)·`sbshop-export.tar.gz`·`_migration_backup_20260714` 삭제 여부(현재 권한만 잠금), 그 안 마켓 인증정보 토큰 회전. Oracle 콘솔 9000 인그레스 삭제.
- 무중단은 스케줄러 리더 선출 이후 재검토. can-agent·life-change 워크플로 표준화.

## 다음 단계 참조
1. 사용자 결정: 위 덤프 삭제/보관, 토큰 회전 범위.
2. (선택) 스케줄러 리더 선출(advisory lock 기반) → 무중단 교체.
3. can-agent·life-change 배포를 같은 구조로 표준화 검토.
