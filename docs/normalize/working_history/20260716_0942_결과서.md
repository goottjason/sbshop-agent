# 정상화 결과서 — 2026-07-16 09:42 · R5 구조 정리·죽은코드·상수화 (전 배치 완주)

REFACTORING-PLAN.md의 권장 순서(R5→R2→R1→…)대로 **R5(구조 정리, 리스크 낮·동작 불변)**를 4개 배치로 완주했다. 전 배치 `./gradlew test` 전건 통과, 관심사별 커밋. push 대기(자동배포=배치중단 방지).

## 이번 사이클 처리 (R5)

### 완료 — 구조 변경(동작 불변)
| 배치 | 결함 | 처리 | 커밋 |
|------|------|------|------|
| B1 | F-ORD-21 | `OrderUpdateCommand.toCustomsData()` 죽은코드 제거(호출처 0) | `bc88bbe` |
| B1 | getAllBatches | `ProcessStatusService.getAllBatches()` 죽은코드 제거(호출처 0) | `bc88bbe` |
| B1 | F-MISC-5 | `EnumMapperValue` → record(JSON 출력 동일) | `bc88bbe` |
| B1 | F-PSRC-10 | `ProductSourcingController` FQCN → import 정리 | `bc88bbe` |
| B2 | F-S3·F-H3 | `OrderService` sourcing·shipping 분기 중복 공통화(+특성테스트) | `625fd0b` |
| B2 | F-ORD-11/20 | confirm/cancel-batch Map 바인딩 → `OrderIdsRequest` record(계약 동일) | `3e5f160` |
| B2 | F-MISC-15 | SSE 두 리스너 → `broadcast` 헬퍼(동작 불변) | `257e6a6` |
| B2 | F-SYNC-16 | preview·carriers `Object` → `Map<String,Object>` 명시화(바디 동일) | `5fec8f7` |
| B3 | F-ORD-32·F-SYNC-4 | 정산율 0.89 3중 하드코딩 → `SettlementPolicy` 상수(값 동일) | `5c0127f` |
| B3 | F-SYNC-21·F-BATCH-M2 | customs 30/1000ms·crawl 500ms → 명명 상수 | `acbc1c6` |
| B4 | F-PROD-10/14 | `ProductUpdateCommand` 26필드 위치기반 → `@Builder` 명시매핑(4곳) | `3241cfc` |

### 오탐(도달가능 확인 → 유지)
- **F-H5** `markSentIfSucceeded` isFailed 분기: `processShipping`·`updateTrackingInfo` 경로는 `failIfNotSent` 선행이 없어 **도달 가능**(실패 경고로그 유일 지점). 죽은코드 아님.
- **F-ORD-10/19** confirm/cancel-batch catch: `@Transactional` 커밋 실패·인프라 오류가 try로 전파 가능 → 제거 시 FAILED 감사로그 유실 = **행위 변경**. 죽은코드 아님.

### 의도/유지(문서화, 코드 무변경)
- **F-BATCH-M3** price=값비교 vs status=파생상태비교: 각 자료형에 맞는 정상 비교.
- **F-BATCH-A3** 배치 3종 정책 차이: crawl(외부호출+스로틀)·manual(입력값)·manualAll(전체필드)은 목적이 달라 타당. 통일 시 오히려 결합.

### R5 범위 밖으로 이관(행위/결정 — 미처리)
- **F-PSRC-16** `toJson` "{}" 삼킴: `Map<String,String>` 직렬화는 사실상 실패 불가(거의 도달불가 방어코드). 관측성 개선은 **R2**에서.
- **F-PSRC-17** `savePending`의 `.productId().sbProductId(productId)` 동일값: sbProductId가 SB상품코드여야 하는지 **도메인 데이터 정합 결정** 필요. 값 교정=행위 변경.
- **F-BATCH-6** couponRate 미적용: "죽은 파라미터 제거" vs "적용 누락(기능 결함)" 판단 필요. 제거는 요청DTO 계약 변경 동반.
- (F-PSRC-16/17 체크리스트 라인은 F-PSRC-14 리팩토링으로 이동됨 — 실경로 갱신 반영.)

## 판정 요약
- 리스크 등급: 전부 **표준 이하**(구조·동작 불변, 계약 보존). 중대(스케줄러·스키마·마켓 계약·다도메인) 없음 → `.autonomous` 마커로 경량·표준 자동 통과.
- 게이트: 각 배치 단독 `./gradlew test` 전건 SUCCESSFUL(core·infra·worker·api, 각 ~2m15s). 병렬 fixer(B2)는 동결 후 리더 단독 게이트로 검증(mid-edit 경합 아티팩트는 리더 재검으로 무효화).
- 커밋: 관심사별 11 커밋(구조 8 + docs 부기 3 · 추적 FINDINGS-CHECKLIST 단일원본). push 미실행.

## 다음 단계 참조

**R5 완주.** 남은 R묶음은 PLAN 권장 순서상 **R2(관측성·활동로그) → R1(오류·HTTP 시맨틱)**.

- **다음 배치 권고: R2 관측성** (리스크 낮). F-SYNC-3(비동기 sync ActionLog), F-ORD-5·15(실패로그 marketType null 조회채움), F-PROD-3·F-ORD-4(조회로그 정책확정), F-SUP-3·CS-5·UC-5·CRED-3(마스터데이터/자격증명 로그), F-MISC-19·F-BATCH-7. **F-PSRC-16(toJson 로그화)도 여기 흡수 가능.** ActionLog 기록 규약을 공통 헬퍼/AOP로 정리하면 반복 제거+일관화.
- **그 다음 R1(시맨틱)**: 404 도입(ResourceNotFoundException)·예외유형 보존(F-ORD-8)·부분실패 표면화. **프론트 계약 영향** 있어 도메인별 사용자 승인 동반(R1은 표준~중대 경계).
- **결정 대기 3건(사용자 판단 필요)**:
  1. **F-BATCH-6** couponRate: 제거(계약변경) vs 마진계산 적용(기능복구)? — 마진 로직 의도 확인 필요.
  2. **F-PSRC-17** sbProductId=productId: 현재 동일값이 의도인지, SB상품코드 별도 주입이 맞는지? — 라이브 데이터 영향.
  3. **정산율 FeePolicy 설정화**: `SettlementPolicy.SETTLEMENT_FEE_RATE=0.89`(flat)를 마켓·카테고리별 FeePolicy(휴면 엔티티) 조회로 이관할지 — 실제 요율표·리포지토리·재무검증 선행. R3/별도 재무 프로젝트.
- **미해결 집계**: R5로 구조 quick-win 소진. 잔여 대량은 R2/R1(관측·계약)·R3/R4(정합·sync 엣지, 라이브 확인 동반)·R7(보안, 방침상 선택)·R8(NOTE 문서).
- **배포 유의**: 이번 변경 전부 동작 불변·계약 보존 → 프론트 영향 없음. 일괄 push 시 기존 유의사항(ddl-auto·배치중단·라이브검증) 동일, 신규 항목 없음.
