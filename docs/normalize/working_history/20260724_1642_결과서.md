# 정상화 결과서 — 2026-07-24 16:42

## 사이클 개요
사용자 요청 4건: [1] 기본 필터에서 취소/반품/교환 제외, [2] 이메일·G마켓/옥션 동기화 실패(빨간불) 검토, [3] 설정 시크릿 평문 표시, (+콤마 표시는 직전 사이클). 이 사이클은 [1][2][3] 처리 + [2] 파생 이메일 버그 수정.

## 처리 결과

### [1] 통합 주문 관리 기본 필터 종결상태 제외 — 완료·배포
- `DEFAULT_VISIBLE_STATUSES`(취소/반품/교환 제외) 신설, 진입 기본 조회에서 종결건 미표시. 유저가 필터에서 체크하면 조회됨. 커밋 6b2f29c, 배포 accdd4f 포함.

### [3] 설정 시크릿 평문 표시 — **배포 차단(안전 가드레일)·원복**
- 구현은 했으나(6b2f29c), `/api/v1/**`가 `SecurityConfig`에서 `permitAll()`인 **무인증 공개 엔드포인트**라, 시크릿·Cafe24 리프레시토큰 평문 응답은 push 시 안전 가드레일이 **credential exfiltration으로 하드블록**(사용자 승인으로도 해제 불가). → accdd4f로 마스킹 상태 원복. 관리자 편의는 **엔드포인트 인증 추가 등 안전장치 마련 후** 재추진 필요.

### [2] 이메일 동기화 빨간불 — 진단 + 수정(D-105)
- **G마켓/옥션(=Cafe24)**: 라이브 로그·DB 진단 결과 **Cafe24 리프레시 토큰이 `invalid_grant`(무효)**. 코드로 복구 불가 → **UI 재인증 필요**. D-103 자동갱신 코드·스케줄러(매일 03:00, `MarketTokenScheduler`→`refreshProactively`, advisory lock, `persist` save)는 정상 구현·배선 확인. **죽은 토큰은 스케줄러도 못 살림** — 재인증으로 새 토큰 심어야 그 뒤 유지.
  - ⚠️ 의심신호: DB상 마지막 갱신 07-23 13:10인데 하루 만에 무효(정상 2주 유효). **회전 desync 또는 같은 Cafe24 client_id 다중 사용(형제 buying-agent 등)** 가능성. 재인증 후 익일 03:00 로그·수일 유지 관찰로 원인 확정 필요.
- **이메일 수집(D-105, 수정완료)**: 아래 별도.

### D-105 이메일 수집 실패 2종 · 표준 · 검증통과
- 버그1: 사용자 편집 필드 `sourcing_order_no`에 한글("재고")이 IMAP `SubjectTerm` SEARCH로 나가 `BAD Could not parse command` 크래시. → `isImapSearchable` 가드로 비ASCII 제외.
- 버그2: `fetchAndProcessEmails` `@Transactional`이 IMAP·마켓 API 느린 I/O 전체를 감싸 DB 커넥션 장기 점유 → `Unable to rollback against JDBC Connection`. → 바깥 트랜잭션 제거(읽기 호출별·쓰기 save별 자체 트랜잭션).
- 파생: N스토어 리네이밍 이후 `ProductControllerActionLogDetailTest` "스마트스토어" 단정 2건 실패 회귀를 "N스토어"로 갱신.
- 테스트: `EmailFetcherOrderNoGuardTest` 4건 + `./gradlew test` 전체 PASS. 수정요지는 원장 D-105.

## 판정·게이트
- 회귀: `./gradlew test` **BUILD SUCCESSFUL**(전 모듈), `frontend npm run build` PASS.
- `.autonomous` 마커 → 표준 자동 통과. push는 승인제.
- 커밋: da86739(콤마)·6b2f29c([1]+[3])·accdd4f([3]원복)까지 배포 완료. d5afd43(D-105 이메일+테스트회귀)는 push 대기.

## 미해결·후속
- **[3] 재추진**: 시크릿 표시는 API 인증(예: 관리자 토큰/세션, nginx basic-auth, 또는 `/api/admin/**`에 인증 부여) 선행 필요. 현재 무인증 공개라 위험.
- **Cafe24 재인증(즉시 사용자 조치)**: 설정 및 연동 → 카페24 → ① 인증 페이지 → 승인 → 리다이렉트 주소 붙여넣기 → ② 리프레시 토큰 발급. 이후 D-103 스케줄러가 유지.
- **Cafe24 토큰 조기무효 관찰**: 재인증 후 하루이틀 내 또 죽으면 회전 저장 desync(코드)로 확정 → refresh 응답 새 토큰 로깅+저장검증 수정. 유지되면 다중 사용(환경 분리) 문제.

## 다음 단계 참조
- d5afd43 push 대기(승인 후 자동배포). 배포 후 라이브 확인: 이메일 `검색 실패`/`Unable to rollback` 로그 소멸.
- Cafe24는 코드가 아니라 **운영 재인증** 대기 — 사용자 조치 항목.
- [3] 재추진 시 API 인증 설계가 선결. 원장 D-105 검증통과, 잔여 백로그는 D-093/094(가격정책), D-070후보 등 별건.
