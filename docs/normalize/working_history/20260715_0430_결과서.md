# 정상화 결과서 — 2026-07-15 04:30 · 잔여 🔴 개별 데이터정합 버그 4건

## 이번 사이클 처리 (병렬 4 fixer)

| 결함 | 처리 | 커밋 |
|------|------|------|
| 🔴 F-BATCH-M1 | 가격/재고 배치 위치(index)결합 → `PriceStockItem` 쌍 객체로 각 값을 productId에 바인딩(백+프론트) | `8d0953b` |
| 🔴 F-PSRC-14 | @Transactional 내 외부 publish 후 save 실패 고아 → PENDING 선저장→publish(tx 밖)→SYNCED 갱신(REQUIRES_NEW 별도 빈) | `50b161c` |
| 🔴 F-MISC-18 | email fetch 수동/스케줄러 동시실행 중복 송장 → AtomicBoolean CAS 재진입 가드(스킵) | `c8e2bb8` |
| 🔴 F-SUP-UC-1·2·3 | POST /currencies 환율 무경고 덮어씀 → 중복 거부(400, 기존 불변)+환율/코드 검증 | `e69496e` |

- **TDD**: 각 재현 테스트 선작성(순서섞임 오적용·저장실패 고아·재진입 중복·중복덮어쓰기 재현). 전 모듈 회귀(api·core·infra·worker) + 프론트 tsc **통합 통과**.
- **리스크 중대**(데이터 정합·마켓·계약변경) → 사용자 승인 후 커밋. **push 미실행**(사용자가 일괄 push 예정).

## 핵심 결정과 이유
- **F-BATCH-M1**: 병렬배열의 위치결합은 근본적으로 취약 → 쌍 객체가 정답. 프론트 호출부는 미사용 래퍼라 형태만 정합. manual-update-all(commands 병렬배열)도 동일 패턴 — 후속 후보.
- **F-PSRC-14**: 외부 부수효과(게시)는 @Transactional로 롤백 불가 → "선-등록(PENDING) 후 외부호출 후 갱신" 패턴으로 저장실패를 복구 가능한 미완료로 전환. 외부호출을 tx 밖으로 빼 커넥션 장기점유(F-PSRC-8)도 완화.
- **F-MISC-18**: 두 트리거가 같은 worker JVM이라 인-JVM AtomicBoolean 가드로 충분. 완전 멱등성은 별개.
- **F-SUP-UC-1**: 사용자 결정 "생성 전용 — 중복 거부". 환율 변경 경로는 미구현(별건).

## 운영 유의 (병렬 fixer)
- 병렬 fixer들이 같은 트리를 편집해 컴파일 상호간섭(테스트 파일 소실·복원) 발생 → 최종 통합 트리를 `--rerun-tasks`로 전량 재검증(통과 확인).
- fixer들이 defect-ledger.md를 무단 수정(2회) → 되돌림. 본 SP 추적은 api-analysis/FINDINGS-CHECKLIST 단일 원본.

## 다음 단계 참조
- **잔여 🔴**: F-BATCH-ST1(/status findAll 메모리 distinct OOM) 정도. 나머지 P0는 해소(또는 정책상 종결).
- **후속 후보**: SP-3 상품(F-PSRC-2·6, F-PROD-12·16, ProductSync 상품별 크롤예외), manual-update-all 쌍객체화, F-PSRC-13 게시 멱등성, F-SYNC-19 customs 동기 sleep, email 완전 멱등성.
- **구조성(P5·P6)**: SP-9(서비스층), SP-11(중복제거), SP-5(응답 DTO), SP-10축소(시크릿 마스킹·접근제어).
- **미배포 커밋**: P4d SSE 이후 이번 4건까지 push 대기(사용자 일괄 push 예정) — push 시 배포 유의사항(ddl-auto update 첫기동·배치중단) 동일 적용.
