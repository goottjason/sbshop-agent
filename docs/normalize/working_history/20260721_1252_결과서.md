# 정상화 결과서 — 2026-07-21 12:52 (KST)

## 트리거
사용자 신고: 배한순·안희수·이호엽·강정란 4명 주문건의 판매가 이상. "배치 업데이트로 마진 10%·할인 15%·최소마진 3500 설정했는데 반영 안 된 듯". 가설 (1) 계산 로직 오류 vs (2) 가격 반영 실패 진단 요청.

## 진단 방법
운영 DB read-only 조회(사용자 승인) + 코드 대조. 스킬: sbshop-normalize(리더 직접 진단 — 라이브 데이터 포렌식 성격).

## 확정 결함
- **D-093 (P1, 중대)** — 가격 배치가 사용자 정책을 무시, 하드코딩 15/20/5000이 실가격 지배.
  - 근본원인: (1) `BatchScheduler`가 매일 05:00 하드코딩 `margin15/coupon20/minMargin5000`으로 전 IHB 상품 재가격(`BatchScheduler.java:34-37`). (2) 수동 배치도 미지정 시 동일 기본값(`BatchController:80-82,152-154`). (3) 마진/할인/최소마진 정책 영속 저장소 부재 → 사용자 수동값은 일회성.
  - 결론: 사용자 가설 **(2) 반영 실패가 정답**. 단 구조적 — 정책 저장소 부재 + 스케줄러가 매일 사용자값을 덮어씀.
- **D-094 (P2)** — `MarginCalculator` 채널수수료 18.5% 전 마켓 고정, `sb_fee_policy`(쿠팡11·스토어8·11번가/G마켓18) 무시.
- **D-095 (계산모델 한계)** — 판매가 = cost_price(크롤)×(1-쿠폰가정) 산정이나 실매입가(sourcing_amount) ±20% 편차. 231108IHB098·220130IHB051 등 실현마진 4~12% 붕괴. minMargin은 '가정매입' 기준이라 원가 과소평가 미보호. 마진 10% 인하 시 저마진 상품 손실 위험 ↑.

## 증거(계산식 실측 검증)
sourcing_amount 보유 최근 16주문 대입 → **현재 DB 판매가 = 15/20/5000 계산값 14/14 정확히 일치**. **계산식(MarginCalculator) 자체는 버그 없이 정상.** 문제는 순수 파라미터.

## 조치
1. **BatchScheduler @Scheduled 비활성화**(가역·주석+사유). 커밋 7602617, main push→자동배포 완료(새 빌드 2026-07-21T03:51:11Z). 향후 고도화(매출 급감 대응) 시 정책 저장소 연결 후 복원.
2. **소규모 라이브 테스트**: 조사 3상품(277·245·3006) 10/15/3500 배치(batchId dd7490d8) → 전건 SUCCESS·4마켓 반영. 277 75900→75000, 245 43200→42200, 3006 24900→24100. 사용자 마켓 실반영 확인 대기.

## 미해결·보류
- D-093 근본해결(정책 저장소 신설) — 사용자가 "수동 배치로 운영, 스케줄러는 나중 고도화" 결정. 정책 저장소는 보류.
- 전체 IHB 10/15/3500 적용 — D-095 저마진 손실위험 감안, 사용자 판단 후 결정(사용자: "우리가 알아서 판단").
- D-094 마켓별 수수료 반영 — 다마켓 단일 sale_price 구조 재검토 수반, 보류.

## 다음 단계 참조
- **사용자 마켓 실반영 확인 대기** — 3상품이 마켓 리스팅에서 75000/42200/24100으로 바뀌었는지. 확인되면 "반영 경로 정상" 종결, 안 바뀌면 마켓별 동기화(D-092 이력 참조) 재점검.
- 사용자가 전체 IHB 적용 결정 시: `/api/v1/products/batch/by-supplier {supplierCode:IHB, marginRate:10, couponRate:15, minMarginPrice:3500}`. **단 D-095 저마진 상품(cost_price 과소평가군) 사전 검토 권고** — 231108IHB098·220130IHB051 등.
- 고도화 착수 시: 정책 저장소(테이블+세 배치경로+스케줄러 연결) = D-093 근본수정. 스케줄러 복원은 이때.
- 스케줄러 disable로 05:00 자동재가격 정지 — 원가·환율 변동 반영은 이제 수동 배치로만. 주기 재가격 필요 판단 시 사용자와 협의.
