# 정상화 결과서 — 2026-07-21 17:48 (D-097 쿠팡 반품완료 전방 감지)

## 트리거
사용자 신고: "쿠팡 김대섭 주문이 배송완료로 표시되나 실제 반품완료 건. 반품완료가 맞으면 정산금액 0 처리."

## 처리 결함
- **D-097 (P1, 중대)** — 쿠팡 반품완료 전방 감지 부재: 배송완료(DELIVERED) 주문의 반품이 RETURNED+정산0으로 전환되지 않던 구조적 공백. **검증통과.**

## 진단 요약
1. **DB 상태 확인**: order_id 33 / li 264 (김대섭, 쿠팡 2101402034506) = DELIVERED, 정산 63,724, verified=false.
2. **쿠팡 원본 검증**(사용자 승인 하 서버 자격증명으로 signed API 호출):
   - 단건 ordersheet 조회 → `400 "해당 주문이 취소 또는 반품 되었습니다"` (반품되면 ordersheet에서 제거됨).
   - returnRequests(timeframe) 조회 → receiptId 1799887551, receiptType RETURN, **receiptStatus RETURNS_COMPLETED**, 김대섭, 고객변심, 완료확정 2026-07-13. → **반품완료 확정.**
3. **근본 원인**: 앱에 쿠팡 returnRequests 호출 경로 자체가 없음. 반품 주문은 ordersheet에서 사라지므로 fetchOrders로 학습 불가. detectCancellations는 DELIVERED를 terminal로 보호(D-027)해 absence로도 안 잡음 → 영구 DELIVERED·정산 고착.
4. **D-027과 구분**: D-027=이미 RETURNED인 주문 오취소 방지(역방향, 기수정). D-097=DELIVERED→RETURNED 전방 전환 신설(별개).

## 핵심 결정과 이유
- **하드코딩 배격(사용자 지시)**: DB만 손대면 다음 동기화가 덮어씀. 특히 정산=0만 두고 DELIVERED 유지 시 정산동기화(DELIVERED건만 sbCode로 revenue-history 조회)가 재부풀림. → **상태를 RETURNED로 전환**해야 지속(반품주문은 ordersheet 부재+terminal 보호로 스킵됨).
- **메커니즘 선택**: absence 추론(“API에 없으니 반품”)은 조회누락·기간초과·취소를 구분 못 하고 오취소 위험. → **returnRequests API로 RETURNS_COMPLETED 권위 확증**. DELIVERED의 취소감지 terminal 보호(D-027)는 그대로 유지.
- **스코프**: RETURNS_COMPLETED·receiptType=RETURN만 전환. 진행중 반품(RU 등)·교환(별 API)은 범위 밖.

## 구현 (커밋 bfe9d2c, TDD)
- `CoupangOrderApiPort/Client.queryReturns` — returnRequests(v4, searchType=timeFrame). 30일을 7일 창 분할, nextToken 페이지네이션, URI.create로 createdAt 콜론 원본 전송(서명 일치).
- `CoupangOrderAdapter.detectReturns` — RETURN·RETURNS_COMPLETED orderId를 DB 대조 → RETURNED+settlement 0+verified. 멱등(RETURNED+0은 재전환 안 함).
- `CoupangOrderSyncService.postSyncProcess` — 취소감지 다음 단계로 detectReturns 배선.
- 테스트: `CoupangDetectReturnsTest` 4건. 회귀: core+infra+api 전체 BUILD SUCCESSFUL.

## 판정·게이트
- 회귀 게이트: 통과(전 모듈 컴파일 + core/infra/api test).
- 리스크 중대(마켓 API 계약 신설) → **사용자 승인 획득**(구현·배포 진행).
- **라이브 검증**: 배포(Started 08:39:53Z) 후 수동 트리거 → 로그 "반품완료 반영: 6건", COUPANG_SYNC SUCCESS. 쿠팡 RETURNED 6건(김대섭 2101402034506·엄혜란·조미경·이경준·이태훈·김동영) 전부 정산 0.00·verified=t. 김대섭 li264: DELIVERED/63724 → RETURNED/0.00. **오취소 0.**

## 부수 효과(긍정)
30일 창의 완료반품 6건이 일괄 자동 교정됨 — 김대섭 외 5건도 DELIVERED 고착에서 해소.

## 미해결·보류
- 없음(D-097 종결). 잔여 참고: D-096 스토어 즉시할인 mop-up(실패 1019건 재실행)은 별건.

## 다음 단계 참조
- **동종 결함(타 마켓)**: 반품/취소/교환 전방 감지 공백은 클러스터 C로 11번가(D-028)·ESM+에도 존재. 쿠팡과 동일 패턴(권위 API 확증 → terminal 전환+정산0)으로 확장 권고. 11번가/ESM+ 반품조회 API 유무부터 조사.
- **정산 정확도(파생)**: 정산동기화가 sbCode(상품) 단위로 revenue-history를 매핑해 동일상품 다주문이 한 값을 공유하는 구조적 부정확성 존재(D-097 범위 밖, 별도 진단 후보).
- **교환(EXCHANGE)**: returnRequests는 RETURN만 처리. 교환은 별 API(exchangeRequests) — EXCHANGED 전방 감지 미구현.
- 원장에 `발견` 상태 잔여 결함 확인 후 다음 배치 판단.
