# 정상화 사이클 결과서 — 2026-08-22 16:45 (D-175 단독 배치)

## 처리 결함

| 결함 | 등급 | 결과 | 커밋 |
|------|------|------|------|
| D-175 상품 500건 상한 — 목록·필터·카운트 부정확 | P1 / 표준(API 계약 변경) | **검증통과 · 배포 · 라이브 검증 완료** | c6fe9a6 |

## 수행 구조

BE fixer(fixer-d175-be) + FE fixer(fixer-d175-fe) 병렬(디렉토리 상호 불가침 계약) → 리더 경계면 판정 1회 → BE 2차 → qa-verifier(verifier-d175) PASS → 리더 전 모듈 회귀 → 커밋·push → 배포 감시 → API 스모크 + Chrome 라이브 검증.

## 핵심 변경

- **BE**: 컨트롤러 2×2 분기(keyword×marketFilter → 유스케이스 3 → 포트 3 → JPQL 4)를 `ProductSearchCondition`(core) + `ProductSpecifications`(JPA Criteria) 단일 경로로 대체. 신규 5축 필터(`categories`·`vendors`·`stockStatuses`·`markets`·`inStockOnly`) 전부 optional·자유 조합. `GET /api/v1/products/categories` 신설. 고아 8심볼 삭제(`grep -ra` 0). 응답 계약·스키마·엔티티 무변경.
- **FE**: ProductGrid 서버 페이지네이션 모드 전환 — 500건 하드코딩·`applyClientFilters`·클라 slice·카테고리 역산 제거, 총건수=totalElements, `paramsSerializer:{indexes:null}`로 반복 키 직렬화 고정, categoryOptions=`/categories`. 외관·배지·모달 불변.

## 핵심 결정과 이유

1. **GMARKET/AUCTION 필터 = CAFE24 등록행의 백필 식별자(`gmarket_goodsNo`/`auction_goodsNo`) 보유로 해석 (리더 판정, 선택지 2)**. 두 마켓은 등록행이 생기지 않는 구조(마켓플러스 핸드오프)인데, 수정 전 클라 필터는 배지 파생 맵을 봤으므로 두 마켓 필터가 실제로 걸렸었다 — 칩 제거(선택지 1)는 회귀라 기각. 키 문자열은 `MarketRegistration` 상수로 승격해 배지·필터가 동일 원천 공유.
2. **운영 JSON은 컴팩트가 아니다 (BE fixer 실측)**: 백필 식별자를 쓰는 writer가 저장소에 없어 운영 DB를 읽으니 `{"gmarket_goodsNo": "..."}` — 콜론 뒤 공백. 컴팩트 가정 LIKE였다면 테스트 전부 green인데 운영 0건이 났을 함정(`compact=0` vs `ws_tolerant=1985`). 공백 무관 LIKE + 운영 Postgres 전수 대조(배지 진실값 대비 불일치 0/3,186행)로 해소.
3. **다중값 파라미터 직렬화가 경계면 1순위 함정**: axios v1 기본은 `categories[]=A`라 Spring `@RequestParam List`에 바인딩 안 됨(필터 조용히 무실행). FE가 `indexes:null`로 반복 키 고정, 검증자가 axios 1.17.0 실 URL 생성으로 실증.
4. 메모리의 갭 3개 중 ①(응답 category 필드)은 착수 시점에 이미 해소돼 있었다 — 착수 전 코드 실측으로 계약을 갱신하고 시작한 것이 유효했다.

## 검증 요약

- qa-verifier PASS(`_workspace/verify/D-175_verdict.md`): git archive 독립 Red 재현(행위 2 FAIL·보존 가드 2 GREEN·계약 400→200), 경계면 8항목 전수 일치, 카운트 질의 경로 자체 프로브로 보강(19/0), 고아 참조 0, 검증자 트리 무변경.
- 리더 회귀: 전 모듈 `./gradlew test` **1,238 tests / 0 failures**(core 766·infra 225·api 175·worker 72) + FE `tsc -p`·`npm run build` 통과.
- 배포: c6fe9a6 push 16:29 → 서버 커밋 일치 + api·frontend 컨테이너 16:31 재생성 + Started 로그 — 3중 확인.
- 라이브: 전체 3,195건 · 64/64 페이지 도달 · G마켓 단독 1,985건(DB 실측 일치) · 비타민 131 → +재고 81 → +FOOD 80건 조합 축소 정확 · 와이어 반복 키 200.

## 비차단 관측 (후속 후보)

1. **카테고리 미분류 2,858행/3,195**: `/categories`가 `["FOOD","UNKNOWN"]`만 반환하고, 패널 "전체 카테고리" 체크 시 null 카테고리 행이 빠져 337건만 남는다(수정 전과 동일 의미라 회귀 아님, 이제 전건에 걸려 체감 큼). 카테고리 백필 또는 "미분류" 필터 옵션이 별도 결함 후보.
2. `vendors` 전체 선택 시 vendor null 행(운영 1행)이 이제 포함 — 계약이 명시 요구한 동작.
3. 식별자 술어는 LIKE 휴리스틱 — 콜론 앞 공백·JSON null에서 배지 파싱과 갈릴 수 있음(현 운영 데이터 0건). 이 키를 쓰는 writer가 생기면 직렬화 회귀 위험.
4. `ProductSearchSpecificationTest`에 작은 pageSize 케이스 1건 추가 권장(count 경로 회귀 가드). / core 테스트 H2 jsonb 벽은 `INIT=CREATE DOMAIN ... JSONB AS JSON` 우회 — Product 다루는 후속 JPA 테스트 공용 힌트. / `getProducts` 파라미터 8개 — 축 추가 시 `@ModelAttribute` 객체화 권장.

## 미해결·보류

없음 (배치 1건 완주).

## 다음 단계 참조

1. **P3 묶음(D-176~180)이 다음 배치 후보** — D-176(날짜 UTC)·D-177(마켓 라벨 3벌, G마켓/옥션 통합 여부 사용자 판정 필요)·D-178(소싱 초안 목록 화면)·D-179(죽은 코드 묶음)·D-180(계약·품질 잔부채). 파일 비중첩 조합으로 3~5건 배치 가능.
2. 위 비차단 관측 1(카테고리 백필/미분류 옵션)은 원장 신규 등재 후보 — 사용자 판단 대기.
3. 운영 가격은 여전히 긴급 세일 상태(IHB 전량 마진1/쿠폰0/200원) — 정상가 복귀는 정책값(15/20/5000) 배치 재실행, 사용자 요청 시.
4. 쿠팡 심사중 4건(3110·3133·2591·3194)·2334 승인반려(WING 확인 필요)는 사용자 대기 항목 유지.
