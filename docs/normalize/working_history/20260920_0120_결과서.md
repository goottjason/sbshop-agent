# 2026-09-20 01:20 결과서 — 배포기 단일화(1단계) 완료

## 처리
| 항목 | 내용 | 커밋 | 상태 |
|------|------|------|------|
| 배포 스크립트 | `ops/deploy.sh`(잠금·디스크·배치 대기·빌드 선행·바뀐 서비스만 교체·prev 태그·헬스체크) + `deploy_test.sh` | 8ed6fa8b | 배포·라이브 검증 |
| 헬스 엔드포인트 | `/internal/health`(DB select 1, 실패 시 503·예외문 비노출) | 32d2a87c | 배포 |
| D-313 | containerd 이미지 저장소에서 이미지 ID가 빌드마다 바뀌어 항상 "변경됨" + 빌드 뒤 옛 이미지 조회 불가 → 내용 지문 + 빌드 전 pending-prev 스냅샷 | 1a859f39 | 라이브 5회 통과 |
| 배포 경로 통일 | `deploy.yml` 이 push·수동 실행 겸용(force/recreate/rollback), 웹훅 삭제·서비스 폐기·9000 규칙 삭제·서버 웹훅 폴더 정리 | 77288f11 | 라이브 |
| D-314·315·316 | 종료코드 6·롤백 사후확인·도중 실패 시 nginx reload·테스트 사각지대 | f7473e81, 416feb99 | 검증 PASS, 실서버 확인 대기 |

## 판정 요약
- 검증자 1차: FAIL(이미지 ID 두 종류) → D-313 수정 후 실서버 5회로 반증·해소(판정서 9절).
- 검증자 재검토 2: 조건부 PASS, 새 결함 1건(D-316) → 수정.
- 게이트: 셸 테스트 168 통과, 변형 26건 사망, 실서버 실행으로 교체·no-op·recreate·rollback·push 확인.

## 핵심 결정과 이유
- 변경 판정은 이미지 ID가 아니라 내용 지문(설정+레이어 해시)을 성공 배포 때 `~/.sbshop-deploy/<svc>.fp` 에 기록해 비교한다 — 이 서버 Docker는 캐시 빌드마다 ID가 바뀐다. 지문은 헬스체크 통과 뒤에만 기록한다.
- 롤백 태그는 빌드 전에 임시 태그로 붙잡아 둔다 — 빌드 뒤에는 옛 이미지가 조회되지 않는다.
- 가짜 도커 테스트는 식별자 종류 차이를 잡지 못한다 → 배포 스크립트는 실서버 실행이 최종 검증이다.

## 미해결·보류
- 사용자: Oracle 콘솔 보안 목록의 9000/tcp 인그레스 삭제.
- 지문을 `.Config` 만으로 계산하는 변형은 가짜 도커로 원리적으로 못 잡는다(실서버 실행이 근거).
- 크론·이메일 수집은 배치 가드 대상이 아니다(락 기반 재시작 안전, 설계 유지).
- 옛 릴리스 폴더 정리(약 1.1GB, 삭제 승인 필요).

## 다음 단계 참조
1. 416feb99 push 후 새 스크립트로 실서버 확인(no-op 1회, 필요 시 recreate) → D-314·315·316 종결.
2. 2단계 착수 전 확인: GHCR 도입(서버 arm64 → arm64 러너 무료 여부, 저장소 공개라 표준 러너·공개 패키지는 무료), 비밀 토큰 범위.
3. can-agent·life-change 워크플로 표준화, 옛 릴리스 폴더 정리(승인 필요).
