# 사이클 7 결과서 — 2026-07-07 (Flyway 전면 제거 — 사용자 지시)

## 처리 내용 (검증통과 → 커밋 06212c0)

사용자 결정: "운영 DB는 데이터가 채워진 살아있는 원본, 직접 관리. Flyway는 아예 걷어내라. 필요해지면 재요청."

- Flyway 의존성 2개(flyway-core, flyway-database-postgresql)·spring.flyway 설정(api·worker)·마이그레이션 V1~V5 제거. 소스 잔존 grep 0.
- **스모크 테스트 가치 보존·강화**: ApiContextLoadSmokeTest는 실 PostgreSQL 기동 검증 유지 (테스트 한정 ddl-auto=create-drop, 운영 none 불변) + 빈 sb_market_credential 기동 내성 검증 추가 — 구 D-016의 빈 DB 기동 실패 시나리오까지 커버.
- MigrationIdempotencyTest·pre_flyway_baseline.sql 삭제 (검증 대상 소멸).
- 원장: D-015/D-016 **무효화** (상태 변경 — 사용자 결정 근거 기록, 레코드 보존).

## 판정 요약

- verifier-c7 PASS (반려 0). 리더 게이트: test+spotlessCheck 전체 그린(23 tasks executed — 캐시 아님), flyway 잔존 0.
- 부수 효과: 사이클 4에서 표시한 배포 리스크("운영 DB에 Flyway 이력 부재 시 충돌")도 소멸.

## 트레이드오프 (사용자 결정으로 수용)

1. **엔티티↔운영 스키마 드리프트를 코드가 탐지하지 않음** — 엔티티 변경 시 대응 DDL을 운영 DB에 수동 적용해야 함 (tdd-bugfix 스킬에 규칙 반영됨).
2. 스모크는 엔티티→스키마 자동 생성으로 도는 구조라 "운영 스키마와의 정합"은 검증하지 않음 (기동·빈 배선·JPA 매핑 유효성은 검증).

## 미검증 (배포 시 확인)

- 외부 CI/CD 파이프라인에 Flyway 실행 단계가 있다면 별도 제거 필요 (레포 내 docker-compose/Dockerfile/deploy 스크립트에는 참조 없음 확인).

## 미해결·보류 (전체 원장 현황)

- 후보(제품 결정 필요): 저장 실패 onError 토스트 부재, 국제 택배사 enum 확충 여부
- 하위: D-007 잔여 lint 6건(로직 재구성 필요), api AsyncConfig 껍데기 정리
- 보류: D-002(오판), D-015/D-016(무효화)
- 수동 확인 대기: 라인아이템 인라인 편집·배송 택배사 저장 (사이클 6 수정분 — 화면 확인)

## 다음 단계 참조

- 남은 항목은 전부 소규모·제품 결정 대기 — 자율 배치 대상 없음. 사용자 지시 대기.
- vitest 도입(프론트 안전망)은 여전히 유효한 제안 — 신규 의존성이라 승인 필요.
- push는 사용자 확인 후 (사이클 7 커밋 3건).
