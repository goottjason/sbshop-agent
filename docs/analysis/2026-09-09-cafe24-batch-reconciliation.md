# 카페24 배치 결과 재검증 및 운영 상태 정정

- 대상: `99c7a7a1-3d25-4565-a790-d65fa8cc3b54` (2026-09-08 아이허브 2,114개 배치)
- 사용자 요청: 카페24 실제 상태를 조회하여 잘못된 실패·보류 표시를 바로잡기.
- 실행: 2026-09-09 11:44–11:49 KST. 카페24 GET만 사용. 마켓 재전송 없음.

## 범위와 결과

카페24 단계 4,228개(상품당 가격·재고)를 조사했다. 실제 작업 대상 1,800개 상품은 상품번호를 지정한 목록 API와 variants를 조회하여 가격, 수량, 판매·재고 설정, 상품번호, SB코드를 확인했다. 수집 실패 등으로 선행 단계에서 제외되거나 연결이 없던 314개 상품은 실행 제외 상태를 유지했다.

| 항목 | 정정 전 성공 | 추가 확인 후 정정 | 정정 후 성공 | 유지한 보류 | 제외 |
|---|---:|---:|---:|---:|---:|
| 가격 | 205 | 51 | 256 | 1,544 | 314 |
| 재고 | 197 | 58 | 255 | 1,545 | 314 |

- 잘못된 결과는 **98개 상품, 109개 단계**였다. 107개는 실패, 2개는 사용자의 재시도 후 상품 변경으로 보류된 `231114IHB044`의 가격·재고였다.
- 109개 모두 원래 하위 작업은 `FAILED_MISMATCH`였다. 목표값을 목록 API로 확인한 뒤 개별 상품 GET으로 다시 확인했고, 재고는 개별 inventory GET까지 추가 확인했다.
- 기존 성공 402개 단계도 현재 목표값과 일치했다. 성공인데 값이 불일치하는 항목은 없었다.
- 유지한 보류: 마켓플러스 연동 1,530개 상품의 가격·재고, 판매 설정 때문에 보류된 가격 14건, 판매·재고 설정 때문에 보류된 재고 15건. 이들은 이번 조회에서도 목표값과 다르므로 성공으로 바꾸지 않았다.
- 카페24 확인을 G마켓·옥션 확인으로 확장하지 않았다.

## 정정 방법과 검증

1. 배치·상품 단계·하위 가격/재고 작업·연결 기록과 GET 근거를 서버의 별도 백업 디렉터리에 저장했다.
2. 계정, shop_no=1, 카페24 상품번호, SB코드, 품목코드, 연결 유지 상태, 판매·재고 설정, 목표값을 대조했다.
3. 정확히 109개 단계만 수정하는 트랜잭션을 먼저 실행 후 ROLLBACK하여 검증했다.
4. 운영 기록이 검토 이후 바뀌지 않았는지 행 잠금과 변경 전 전체 행 비교로 검사했다. 동일 연결에 CHECK/VERIFY 작업이 없고 조회 근거가 15분 이내인 경우에만 정정을 허용했다.
5. 하위 작업을 `CONFIRMED_PRICE` / `CONFIRMED_QUANTITY`, 배치 단계를 `SUCCEEDED`로 정정했다. 확인값과 확인 시각을 저장하고 `READ_RECONCILED` 및 배치 처리 이력을 추가했다. 과거 실패 이력과 기존 전송 횟수는 보존했다.
6. 영향을 받은 98개 상품의 집계를 기존 애플리케이션과 같은 우선순위로 다시 계산했다. 다른 마켓의 실패·보류는 유지했다. 상품 원본 필드, 마켓 연결 상태, 전체 필드 동기화 플래그는 수정하지 않았다.
7. 정정 이후 전체 카페24 단계와 3,600개 가격·재고 하위 작업을 비교하여 범위 외 단계·작업이 바뀌지 않았고 전송 횟수도 변하지 않았는지 확인했다.
8. 운영 API 및 독립 Selenium Chrome에서 `231114IHB044`의 가격·재고가 모두 ‘반영 확인’, 목표값=확인값 32,400원 / 300개로 표시되는 것을 확인했다. 브라우저의 비조회 요청을 차단한 상태에서 검사했고 JS 오류는 없었다. 기존 마켓플러스 로그인 브라우저는 사용하지 않았다.

화면 문구: `재확인 완료: 카페24 판매가 32,400원, 배치 목표와 일치합니다. 재전송 없이 확인했습니다.`

전체 배치 집계는 성공 95, DB만 완료 3, 실패 347, 보류 1,669다. 카페24를 정정해도 다른 마켓에 보류가 있으면 상품 전체는 보류로 남는다.

## 한계 및 보존 근거

이번 작업은 해당 배치의 현재 실제 값에 따른 **운영 결과 정정**이다. 어젯밤 조회가 이전 값을 반환한 정확한 원인은 아직 확정되지 않았으며, 자동 확인 로직 자체는 변경하지 않았다. 최초 성공 시점을 소급하여 꾸미지 않고 오늘 실제 확인한 시각을 남겼다. 애플리케이션 재배포 없이 운영 기록에 적용했다.

서버 근거: `/home/ubuntu/backups/sbshop-cafe24-reconcile-20260909/`

- `before.json`, `products.json`, `requests.json`, `audit.json`
- `verified.json`, `verification-requests.json`
- `repair-dry-run.sql`, `repair-dry-run-result.json`
- `repair-apply.sql`, `repair-apply-result.json`
- `after.json`, `price-tasks-after.json`, `stock-tasks-after.json`, `summary.json`
- `operating-browser-result.json`, `operating-browser.png`

[카페24 공식 Admin API 문서](https://developers.cafe24.com/docs/en/api/admin/)의 상품번호 복수 조회·variants embed·개별 상품/품목 재고 조회를 참고했다. 삭제·수정 API는 사용하지 않았다. API 인증 토큰은 기록물에 저장하지 않았다.
